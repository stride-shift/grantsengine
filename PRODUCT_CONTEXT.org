#+TITLE: Product Context — d-lab Grant Engine
#+DATE: 2026-06-22

* Purpose
Present-state product context. Dormant/deferred features live in DEFERRED.org.
Read this before audits and before flagging code as an "issue" — several
properties below are intentional.

* What It Is
An internal tool for d-lab NPC's grant team to manage the funding pipeline:
scout opportunities, vet and shortlist them, draft AI-assisted proposals and
funder research, build budgets matched to d-lab's 8 programme types, track
deadlines, and store supporting documents. It is multi-tenant by organisation
slug but used primarily by one org (d-lab).

* Users & Roles
| Role | Scope | Can do |
|------|-------|--------|
| Super admin | All orgs | Provision orgs (create/delete) via the hidden =?superadmin= route, gated by =SUPER_ADMIN_KEY= |
| Org admin | One organisation | Manage members, org profile/onboarding, all grant + AI features |
| Member | One organisation | Work the pipeline: scout, vet, draft, budget, track deadlines |

Members sign in with **email + password**; the server resolves their org from a
globally-unique email (no org picking). Passwords are bcrypt-hashed; reset is
email-based via Resend. A legacy per-org member-picker login remains as a
migration fallback until every member has a backfilled email.

* Feature Lifecycle
| Feature | Status | Note |
|---------|--------|------|
| Pipeline + stage gates | Active | STAGES/GATES in constants.js; per-stage gate criteria |
| Grant detail + vetting | Active | GrantDetail, Vetting; inline apply-URL edit |
| AI proposal drafting | Active | Inline prompts in App.jsx via OpenAI proxy |
| Funder research / intel | Active | Funders view; web-search via gpt-4o Responses API |
| Grant scouting | Active | ScoutPanel + scoutPrompt; server scout job + scraper |
| Server-side scraper | Active | sources.js, scrapeGrants.js; Anthropic Haiku parse/verify |
| Budget builder | Active | Programme-type matched budgets (detectType) |
| Proposal workspace + .docx export | Active | ProposalWorkspace, docxGenerator.js; per-section readability enforcement (rewrites worst sentences) |
| Apply-page autofill (detect / map) | Active | AutoFillPanel/useAutofill: fetch apply page → detect form fields + required docs → map to org/grant + the finished draft; manual copy-paste always available |
| Playwright fill + submit | Active (optional) | server/routes/autofill.js + playwright-service; gated by PLAYWRIGHT_SERVICE_URL/SECRET; form-only; degrades to copy-paste when unconfigured |
| Full proposal automation | Active | "✨ Auto-draft & prepare" in GrantDetail: research → draft (+ readability) → review with required-docs checklist + pre-filled form |
| Document vault + uploads | Active | DocVault, UploadZone; URL/file/YouTube extraction |
| Deadline reminders | Active | server/jobs/deadlineReminders.js (cron + email) |
| Calendar / conferences / freebies | Active | Calendar, Freebies; conference + freebie data |
| Org profile + onboarding | Active | WelcomeTutorial, TourOverlay; OrgSelector is now the fallback picker, not the default login |
| Edit-learning | Active | editLearner.js adapts to user edits of AI output |
| Admin panel | Active | Admin.jsx member + org management |
| Email+password login | Active | Primary path: =POST /api/auth/login= → org auto-resolved; EmailLogin/useEmailLogin |
| Org provisioning (super-admin) | Active | Hidden =?superadmin= route + =SUPER_ADMIN_KEY=; create/list/delete orgs |
| Email backfill tooling | Active | =server/backfill-emails.js= — dry-run audit + collision-safe apply + setup links |

* Intentional Decisions (do not re-flag)
| Decision | Choice | Rationale |
|----------|--------|-----------|
| Monolithic App.jsx (~2400 lines) | Kept as one file | Views tightly coupled to shared grant state; splitting deferred (see DEFERRED.org) |
| prompts.js mostly LEGACY | Only scoutPrompt imported | App.jsx inlines its own prompts; LEGACY exports kept as reference, not dead by accident |
| grant-engine.monolith.jsx | Kept in src/ | Working single-file reference that runs in Claude artifacts — intentional, not stale |
| No TypeScript | Plain JSX | Project predates a TS migration; not a defect |
| No router | =view= state toggle | Single-page scope; router deferred |
| Inline styles, no CSS framework | Design tokens in theme.js | Enables standalone single-HTML demo build |
| CSP disabled in Helmet | =contentSecurityPolicy: false= | Vite injects inline scripts; intentional in app.js |
| ZAR currency, B-BBEE/SETA/NPO domain terms | Throughout | South African NPO context — not typos or i18n gaps |
| Email login primary; org auto-resolved | Single email+password screen, no org picker / admin-key field | Org resolved server-side from a global-unique email (=getOrgAndMemberByEmail=) |
| Legacy member-picker kept as a fallback | "Use member sign-in" link + =?superadmin= picker | Transitional until every member has a backfilled email — not dead code |
| Org creation = super-admin only | Hidden =?superadmin= route + =SUPER_ADMIN_KEY= header | Regular users never see create/delete; create/delete gated server-side |
| Auto-submission is opt-in, never automatic | Playwright fill + submit are explicit, confirmed, form-only, service-gated | The one-click "Auto-draft & prepare" flow stops at a fully-prepared review; submitting to a funder is irreversible, so a human always confirms (=handleFinalSubmit= confirm dialog) |

NOTE: email login can only be a member's sole path once their email is backfilled
(=server/backfill-emails.js=); until then the member-picker fallback covers them.

* Stable Reference Data
Org/funding facts (financial model, GIDF cornerstone funder, 8 programme types
with exact budgets, relationship owners, proposal strategy) are canonical in
=CLAUDE.md= and =src/data/context.js=. Do not duplicate them here.

---
/Last Updated/: 2026-06-23
