<!-- scope=src | DRY RUN — zero source files modified | re-scout targets: 21 modules | total findings: 187 | human-global: 62 | flow-auto: 67 | flagged: 58 -->
<!-- Generated: 2026-06-22 | Model: claude-sonnet-4-6 | No verify committee — human intent/false-positive screen required before any fix is applied -->

# Micro Code-Health Audit — src/ (2026-06-22)

## Summary

This is a **dry-run audit** of the full `src/` tree across 21 modules (api.js, utils.js, App.jsx, useAI.js, editLearner.js, prompts.js, docxGenerator.js, funderStrategy.js, constants.js, context.js, glossary.js, Pipeline.jsx, GrantDetail.jsx, ProposalWorkspace.jsx, BudgetBuilder.jsx, ScoutPanel.jsx, Vetting.jsx, Funders.jsx, Dashboard.jsx, DocVault.jsx, UploadZone.jsx, AutoFillPanel.jsx, Admin.jsx, Login.jsx, Settings.jsx, OrgSelector.jsx, Calendar.jsx, WelcomeTutorial.jsx, SectionCard.jsx, index.jsx/components, and the two test modules). **Nothing was applied.** There was no automated verify committee; every finding below requires a human intent and false-positive screen before action. The audit surfaced 187 findings: 62 require human judgment (logic bugs, architecture decisions, data-integrity risks), 67 are mechanical auto-fixable cleanups, and 58 sit at the edge-of-logical boundary. Two test-suite findings are currently **RED** (failing tests in `__tests__/funderStrategy`).

---

## Needs Your Judgment (human-global)

These findings are logical, stateful, or cross-cutting. They may be correct code that reflects an undocumented intention — screen each one before acting.

| Module | Location | Issue | Blast Radius | Coupling | Stateful? | Test Coverage | Suggested Fix |
|---|---|---|---|---|---|---|---|
| **src/api.js** | :588-600 | `runAutofill`/`submitAutofill` pass `_skipOkCheck:true` then call `res.json()` unconditionally. Non-JSON error bodies throw an unhandled parse error instead of a meaningful message; autofill jobs may be left in ambiguous server-side state. | Autofill job execution — confusing parse exception on any server/network error | Medium — callers in App.jsx break on unexpected throw | Yes — server job state ambiguous | Unknown | Either remove `_skipOkCheck:true` so `f()` handles non-OK, or add explicit `res.ok` check + error extraction before `res.json()` |
| **src/api.js** | :66-101 | `login`, `setPassword`, `memberLogin`, `resetPasswordWithToken` duplicate the same raw-fetch + `res.json()` error extraction pattern four times. Any normalisation change (e.g. error field rename) must be applied in four places. | Auth flow — all login/reset entry points | Medium — four exported functions share identical structure | Yes — all call `setAuth()` mutating module-level state and localStorage | Unknown | Extract a `preAuthPost(url, body)` helper and wire all four through it |
| **src/api.js** | :379-446 | `api()` is 67 lines with three abstraction levels inside one loop: transport/retry, status-to-user-message mapping, and response parsing. Changing retry policy requires touching message-mapping code. | Every AI prompt call in the app — behaviour-preserving if extracted cleanly | High — every AI feature calls api() | No | Unknown | Extract `parseAiResponse(res)` helper for lines 413-436; leave the loop responsible only for retry/backoff |
| **src/utils.js** | :475 | `grantReadiness` nextAction doc-missing check builds the wrong string. Condition tests `` `${required?.length || 0} docs missing` `` but the pushed string is `` `${gap} docs missing` `` where `gap < required.length`. Grants with partial docs skip to "Submit draft for review" instead of "Upload missing compliance documents". | All grants in 'drafting' with partially-uploaded docs show wrong next action | Consumed by Dashboard:496, Pipeline:1291, GrantDetail:566,673,744,1691 | No — wrong label returned only | Not tested | Change line 475 to `missing.some(m => m.endsWith('docs missing'))` or track a separate `hasDocGap` boolean at push time |
| **src/__tests__/utils.test.js** | :1-193 | Five critical exports — `grantReadiness`, `deadlineCtx`, `validateProposalBreaks`, `readabilityScore`, `detectSubmissionMethod` — have zero test coverage. Includes the logic bug at utils.js:475. | Silent regressions in all pipeline readiness scoring, deadline urgency, proposal quality checks, submission method detection | All five consumed by Dashboard, Pipeline, GrantDetail, Calendar, ProposalWorkspace, AutoFillPanel | Pure functions; no risk in adding tests | None | Add describe blocks for all five; reproduce grantReadiness:475 bug with a test first so the fix is verifiable |
| **src/__tests__/utils.test.js** | :41-44 | Test asserts `parseStructuredResearch({ priorities, contacts })` returns `null` but the implementation's `looksLikeResearch()` returns `true` for those keys and `synthesizeRawText()` constructs a non-null result. Test misrepresents the contract and the synthesis path is untested by any passing test. | Misrepresents parseStructuredResearch contract | utils.js:parseStructuredResearch (looksLikeResearch + synthesizeRawText) | No | Only test for this path — currently misrepresents it | Change input to one with no RESEARCH_FIELDS keys (e.g. `{ foo:'bar' }`), or rename test and assert synthesized value |
| **src/__tests__/utils.test.js** | :1-193 | `grantReadiness` is entirely untested (76 lines, three weighted sub-scores, stage-specific nextAction branching). | Grant readiness score on every grant card; drives user next-action copy and pipeline qualification | utils.js:grantReadiness, constants.js, GrantDetail | Pure function | Zero | Add describe block: all-missing → score 0; all-present → score 100; one case per sub-score bucket; stage-specific nextAction for scouted/qualifying/drafting/won |
| **src/__tests__/utils.test.js** | :1-193 | `validateProposalBreaks` is entirely untested (two module-level constants, multiple code paths). | Proposal quality gate shown to grant writers | utils.js:validateProposalBreaks, ProposalWorkspace | Pure function | Zero | Tests for: short text → score 100; long prose without breaks → score < 100; text with all break types → score 100 |
| **src/App.jsx** | :390-634 | Background hygiene `useEffect` has `grants` in its dependency array (line 634), causing the effect closure to be re-registered on every grant mutation. The ref-gate prevents body re-execution but the guard code runs on every of potentially many state updates the hygiene job itself triggers. | Every grant mutation re-registers this effect; misleading semantics; fragile if ref is reset | Depends on runAI, currentMember, dSave, verifyUrls, isAIError, all grant fields | Yes — writes to grants state and Supabase via dSave | None | Replace `grants` in dep array with `grants.length > 0` evaluated outside, or extract hygiene logic into a ref-stable callback triggered once after first non-empty load |
| **src/App.jsx** | :415-633 | 200+ line anonymous IIFE inside `useEffect` contains four sequential pipeline stages (regex cleanup, funder dedup, AI URL resolution, AI brief fetch) with mixed abstraction levels. Untestable and hard to modify in isolation. | High — IIFE mutates grants state and calls Supabase for every active grant on each session | Deeply coupled to grants, runAI, dSave, setGrants, many utilities | Yes — writes to grants state and Supabase | None | Extract each pass as named async function (`runNotesHygiene`, `runDedupePass`, `runUrlHygiene`, `runBriefFetch`); effect body becomes a 5-10 line orchestrator |
| **src/App.jsx** | :495 | Pass 3 `liveGrants` filter reads `initialGrants` (pre-hygiene snapshot) for stage field. Grants auto-archived in Pass 1.5 still hold their old stage in the snapshot, pass the `CLOSED.has(g.stage)` filter, and receive unnecessary AI URL calls. | Spurious AI URL-fix calls for expired grants — latency and rate-limit cost | persist function, working Map, initialGrants snapshot, CLOSED set | Yes — makes external AI and URL-verify network calls for grants that should be skipped | None | Build `liveGrants` from `working.values()` instead of `initialGrants` so stage changes from earlier passes are reflected |
| **src/hooks/useAI.js** | :78 | `runAI` (~1460 lines, 15+ prompt types) runs the full `orgCtx` assembly unconditionally before type branches, including up to 3 async API calls for uploads, learnings, and proposal library. Types like `urlextract`, `extractEmailFeedback`, `winloss` never use orgCtx but pay the full cost. | Every AI call fires at least one extra await (getUploadsContext) when it need not — latency hit for 8 of 15 prompt types | Tightly coupled to api.js (getUploadsContext, getUploadsByCategory, getUploadFull, kvGet) and editLearner.js | Yes — writes into three useRef caches | None | Gate expensive context assembly behind `needsOrgContext` or a broader `needsFullCtx` guard; types `urlextract`, `extractEmailFeedback`, `findApplyUrl`, `fetchFunderBrief`, `extractNotes`, `winloss` do not need the uploads pipeline |
| **src/hooks/useAI.js** | :517-519 | `runAI` signature `(type, grant, priorResearch, priorFitScore)` reuses positional args with completely different semantics per type. For `sectionDraft`, `priorResearch` carries a destructured options bag; for `urlextract`, it carries a URL string. Undiscoverable without reading all 1460 lines. | All 15+ call sites in App.jsx; pure signature change, zero runtime semantic change | Coupled to every App.jsx caller | No | None | Accept a single options bag: `runAI({ type, grant, ...opts })`; breaking change to all call sites but eliminates ambiguity |
| **src/editLearner.js** | :171-173 | Error-string prefix detection is brittle: guards `startsWith("Error")`, `startsWith("Rate limit")`, `startsWith("Connection")` but misses `"No response — try again."` and `"Request failed after multiple retries"` returned by `api()`. Those strings are stored in KV as valid writing rules. | Corrupt rules injected into every future AI draft prompt; degrade all proposals until manually cleared. KV state persists across reloads. | Depends on api.js error-return contract which already changed once (Gemini→OpenAI) | Yes — writes to persistent org-scoped KV | None | Replace multi-prefix check with `isApiError(s)` regex: `/^(Error|Rate limit|Connection|No response|Request failed)/i.test(s)` |
| **src/editLearner.js** | :43-46 | `kvGet` errors silently swallowed; `existing` stays `null`. A transient network error is indistinguishable from "no learnings yet" — `mergeLearnings` takes the first-edit path and discards all previously accumulated rules. | Destroys accumulated KV writing rules on any transient server blip; rules not recoverable without DB restore | kvGet/kvSet in api.js | Yes — overwrites persistent org-scoped KV | None | Only proceed to `mergeLearnings` when `kvGet` succeeds; on failure, return early rather than proceeding with `existing = null` |
| **src/data/funderStrategy.js** | :21 | `isFunderReturning` returns `true` for empty-string and null input because `"".includes("")` is true. Grants with no funder name are silently classified as returning funders, triggering returning-funder framing in AI prompts and UI badges. | Dashboard returning badge, Funders view, every AI prompt that branches on returning — all misfire for blank funder names | Imported in Dashboard.jsx, Funders.jsx, useAI.js; fix auto-corrects all callers | No — AI prompt content affected | Test at line 38-42 asserts wrong outcome (toBe(true)) — bug is test-blessed | Add `if (!n) return false;` at top of arrow function before `.some()` call |
| **src/data/funderStrategy.js** | :46 | `detectType()` amount-inference ladder never returns PTYPES[8] (Bespoke Corporate, R2.75M). Asks above R1.4M fall through to PTYPES[2]. `selectOptimalBudget()` can pick Type 8 via keyword path — two detection paths are inconsistent. | BudgetBuilder auto-detection, useAI.js prompt drafts, GrantDetail display — any large-ask corporate grant gets wrong budget tables and programme description in generated proposals | detectType imported by BudgetBuilder, useAI, GrantDetail, ProposalWorkspace, Pipeline | No DB writes directly, but output seeds budget tables user saves | Test suite has no test for ask at PTYPES[8] cost | Document intentional exclusion with comment if Type 8 is always keyword-only, OR add `if (g.ask >= 2500000) return PTYPES[8];` before PTYPES[2] fallback; make `selectOptimalBudget` consistent |
| **src/data/funderStrategy.js** | :188 | `funderHasFullStructure` heuristic silently discards custom sections for any entry with fewer than 4 sections or without 'cover'/'budget' keywords. Momentum Group (3 sections) and SAP (3 sections) fall through to generic Foundation structure. No warning. | All AI proposal drafts for Momentum Group and SAP receive wrong section scaffold | funderStrategy() only — fix is local | No — AI prompt structure affected | No test covers Momentum Group or SAP specifically | Replace heuristic with explicit `useFullStructure: true` field on `angles` entries, or always use funder-specific sections when present |
| **src/data/funderStrategy.js** | :117-166 | Six pairs of near-identical alias entries in `angles` (Get It Done Foundation/Get It Done, DGMT/DG Murray Trust, etc.) are copy-pasted. Copies are already slightly diverged (GIDF hook length differs). A content update to one will not propagate. | AI proposal hooks/framing wrong for any diverged alias after a one-sided update | funderStrategy() only | No | None | Define canonical entry once; add alias: `angles['Get It Done'] = angles['Get It Done Foundation'];` after the literal |
| **src/data/constants.js** | :19-21 | `WFAC`, `LFAC`, `TOPICS` exported but never imported in the live app. Only used in `grant-engine.monolith.jsx` which has its own local copies. Win/loss factor pickers in the live app are free-form strings from `g.on` with no structural coupling. | Zero runtime impact today; could mislead future devs into thinking these are live data | Dashboard.jsx uses g.on as freeform text; no structural coupling | No | None | Determine whether live-app components should import these; if monolith-only, mark Intentionally Dormant in DEFERRED.org with a comment |
| **src/data/constants.js** | :261-275 (App.jsx) | `DEFAULT_STAGES` in App.jsx duplicates `STAGES` from constants.js but omits the 'vetting' stage (11 vs 12 stages). `DEFAULT_FTYPES` is an exact duplicate of `FTYPES`. Any org that loads before `getPipelineConfig` resolves will not show the vetting column. | High — stages prop passed to Pipeline, GrantDetail, every stage-aware component; missing 'vetting' affects pre-load render for every user | Pipeline.jsx:107 shadows with its own local STAGES; all stage-aware children consume stages via prop | Yes — stages is React state; initial value matters for first render | None | Import STAGES and FTYPES from constants.js; use them as useState fallback values (confirm whether 'vetting' omission is deliberate before collapsing) |
| **src/data/constants.js** | :Admin.jsx:6-11 | Admin.jsx defines local `ROLES` array (4 entries, shape `[{id,label,desc}]`) that diverges from constants.js ROLES (6 entries, shape `{[roleKey]:{label,level,can[]}}`). Admin.jsx omits 'coord' and 'comms' — coordinators and comms staff cannot be assigned in the admin member UI. Label strings also differ ('Admin' vs 'Director'). | Medium — affects admin member-management UI dropdowns; does not affect pipeline gate logic | constants.js ROLES used in Pipeline.jsx for gate level checks; Admin.jsx ROLES used only in member form dropdowns | No | None | Decide canonical role list; if intentionally limited to 4, document; if coord/comms should be assignable, add them; resolve label mismatch |
| **src/data/constants.js** | :99-106 | `GATES` has no entry for 'qualifying->drafting'. Any user can advance from qualifying to drafting with a single button click and no approval check. | Medium — affects stage progression for all active grants; any user can bypass a missing gate | Pipeline.jsx GateIndicator, utils.js:477, GrantDetail.jsx:1789 | Yes — grant.stage transitions persisted to Supabase | None | Decide whether qualifying->drafting should be gated; if intentional, add comment in GATES; if it should be gated, add `{'qualifying->drafting': {need:'hop', label:'...'}}` |
| **src/data/context.js** | :1-189 | `CTX` and `CTX_SLIM` are exported but never imported in the live app. `useAI.js:83` reads `profile.context_slim` from the database. Editing this file has zero effect on AI output. CLAUDE.md instructs "Update org context: Edit src/data/context.js" — that instruction is wrong. | High — every AI prompt believes it receives canonical org identity; the actual source is a DB value that may be diverged. Updating this file fixes nothing. | Fully decoupled from live call path; CLAUDE.md, ARCHITECTURE.org, DOCUMENTATION.md all reference it as canonical source — false editorial contract | Yes — live context is in org_profiles.context_slim in DB, editable via Settings.jsx | None | (a) Import CTX/CTX_SLIM as the initial DB seed so this file is canonical, OR (b) document explicitly that it is a reference copy and correct CLAUDE.md/ARCHITECTURE.org/DOCUMENTATION.md |
| **src/data/context.js** | server/seed.js:14-21, server/seed-fn.js:12-19, server/migrate-context.js:15-22 | Three server-side files inline their own CTX_SLIM and CTX_FULL that are materially diverged: 7 programme types (not 8 — Type 8 missing), wrong Inkcubeko location ('Gauteng' vs 'Western Cape — George'), stale budget figure (R13.4M vs R14.16M), outdated mission tagline. These seed the DB that all live AI calls read from. | High — stale DB seed is what AI proposal prompts actually receive; proposals drafted via the app will reference 7 programme types and wrong Inkcubeko location | Directly coupled to production AI output via useAI.js profile.context_slim | Yes — DB record persists until manually updated via Settings or migration | None | Import CTX and CTX_SLIM from src/data/context.js in all three server files; remove inline copies; run migration to bring live DB record up to date |
| **src/components/Pipeline.jsx** | :718-782 | URL extract logic duplicated between `onKeyDown` handler (lines 718-746) and `Btn onClick` (lines 750-780). Button path uses `alert()` on error; Enter-key path uses `onToast`. Inconsistent error surface; grant object shape must be maintained in two places. | Two code paths diverge over time; any grant shape change requires two edits | Calls onRunAI, onAddGrant, onToast — all critical-path props | Yes — sets urlBusy, urlInput, showUrlTool; onAddGrant mutates grants list | None | Extract `runUrlExtract` async function; wire both paths to it; use onToast consistently; remove alert() at line 774 |
| **src/components/Pipeline.jsx** | :31 | `STAGE_ORDER` in GateIndicator is a local constant listing 7 stages, duplicating canonical STAGES (12 entries). 'resubmit' is absent from STAGE_ORDER — gate indicators are suppressed for any grant in that stage. | Any grant in 'resubmit' stage shows no gate indicator — silent correctness gap on gate enforcement | GateIndicator renders on every kanban and person-view card | No — read-only display | None | Derive STAGE_ORDER from imported STAGES array filtered to stages that have gate entries in GATES: `const gatedStageIds = new Set(Object.keys(GATES).flatMap(k => k.split('->')))` |
| **src/components/Pipeline.jsx** | :944 | Custom programme IDs generated as `custom-${prev.length}`. If user adds two items (custom-0, custom-1), deletes custom-0, then adds a third, it gets custom-1 — colliding with the surviving entry. Duplicate ID causes cohort multiplier selection to apply to both programmes via Map key. | Users who add, delete, and re-add custom programmes get silent ID collisions corrupting selected programme map and final ask calculation | selectedPTypes Map uses this ID as key; calcTotalAsk and buildPtypeNotes iterate over it | Yes — customProgrammes and selectedPTypes state depend on stable IDs | None | Use `uid()` (already imported) to generate the custom programme ID |
| **src/components/GrantDetail.jsx** | :420 | `aiLog` defined in render body captures `grant` from the prop parameter. Async callbacks (`runFitScore`, `rollTheDice`, auto-brief `useEffect`) may fire after the component receives a new `grant` prop, holding stale `g.log` and `g.id`. Log entry appended to wrong grant or with stale log baseline — duplicate or missing activity-log lines. | Activity-log data integrity for all AI operations on this grant | All async AI call-sites inside GrantDetail: runFitScore, runResearch, rollTheDice, auto-brief useEffect, followup, winloss, conceptNote | Yes — mutates persistent grant.log in Supabase via onUpdate | None | Wrap `aiLog` in `useCallback` with `[grant, onUpdate]` deps, or pass grantId and current log as explicit parameters at each call site |
| **src/components/GrantDetail.jsx** | :346 | `_pendingAI` effect's `runPending` function duplicates the research-fetch block that already exists in `runResearch`. The two copies differ: `runPending` calls `storeResearch` without calling `aiLog`. Future changes to the research path must be applied twice; the missing `aiLog` call means activity log won't record the research step when triggered by the pending-AI path. | Affects grants auto-opened from '+Add' wizard with `_pendingAI.draft` flag; missing aiLog call produces incomplete activity log | runResearch (useCallback at line 403), _pendingAI useEffect (line 339), activity log | Yes — writes aiResearch to Supabase | None | In `runPending`, replace the inline research-fetch block with a call to `runResearch()` directly |
| **src/components/ProposalWorkspace.jsx** | :104-133 and 205-231 | Section-result persistence logic duplicated verbatim between `generateSection` and `generateAll`. Both maintain the 3-entry history ring buffer and build the identical section-shape object. `generateSection` carries `customInstructions` from the call arg; `generateAll` reads it from `prev.customInstructions`. The two paths can produce different section shapes for the same event — latent inconsistency propagating to every consumer of `g.aiSections`. | Any inconsistency in saved `aiSections` entry propagates to SectionCard, preview renderer, assembleText, docx export, editLearner, backward-compat aiDraft assembly | Tight — both write to g.aiSections via onUpdate, persisted to Supabase | Yes — persisted to DB | None | Extract `buildSectionEntry(prev, cleaned, customInstructions?)` pure helper; call from both functions |
| **src/components/ProposalWorkspace.jsx** | :146-284 | `generateAll` sets `busy.generateAll = true` and `generatingAllRef.current = true` at start, but the post-loop reset code has no `try/finally` guard. Any unhandled throw in `assembleText`, `extractAskFromText`, or `onUpdate` leaves the component permanently stuck in the spinner with no user recovery short of page reload. | Unrecoverable UI busy state; Stop button only resets generatingAllRef, not busy.generateAll | Self-contained, but busy state owned by parent via setBusy | Yes — generatingAllRef and busy both permanently true | None | Wrap lines 247-283 in `try { ... } finally { setBusy(p => ({...p, generateAll: false})); generatingAllRef.current = false; }` |
| **src/components/ProposalWorkspace.jsx** | :494-497 | `DownloadBtn` `onDocx` callback silently ignores the `text` parameter and calls `exportDocx()` which re-assembles from sections internally — without applying the glossary appendix. When `g.includeGlossary` is true, the .docx file will not contain the glossary even though the copy-to-clipboard path does include it. | Limited to docx export — glossary silently omitted from .docx when user has opted in | exportDocx and buildGlossaryAppendix | No — download only | None | Pass `assembledText` (which includes optional glossary) into `exportDocx`, or refactor `exportDocx` to accept a pre-assembled string override |
| **src/components/BudgetBuilder.jsx** | :108-135 | `saveBudget` writes `aiRecommendedAsk: calcs.total` unconditionally alongside `ask: calcs.total`. These are semantically different fields. Overwriting `aiRecommendedAsk` with the builder's computed value every save destroys any prior AI-recommended ask that differed, with no user confirmation. Affects every AI prompt that references `aiRecommendedAsk`. | Any AI proposal draft referencing aiRecommendedAsk receives budget-builder total instead of AI-generated recommendation after any budget save | Writes to ask, askSource, aiRecommendedAsk, askYears via onUpdate → Supabase PUT | Yes — persisted to Supabase | None | Either remove `aiRecommendedAsk` write from saveBudget (builder is canonical when used), or add comment explicitly documenting that builder takes precedence |
| **src/components/BudgetBuilder.jsx** | :62-73 | `loadTypeItems` filters out the TOTAL row by label string match (`label !== 'TOTAL'`). If any PTYPES entry changes the TOTAL row label casing, the TOTAL line appears as a user-editable line item and inflates the calculation. The sentinel contract is not enforced anywhere. | If sentinel breaks: TOTAL row appears as editable line item, double-counts in calculation, produces inflated budget saved to budgetTable and injected into AI prompts | BudgetBuilder↔PTYPES.table row format; informal string contract | Yes — affects items state and all downstream calculations including persisted budgetTable | None | Filter by structural marker: add `isTotalRow: true` to PTYPES table entries, or always drop the last row, or add JSDoc to PTYPES stating the TOTAL sentinel contract |
| **src/components/ScoutPanel.jsx** | :224-231 | Local `isHomepageOnly` duplicates `utils.isHomepageOnly` but omits the `/en` path check (utils.js:118). URLs like `https://example.org/en` are mis-classified as a non-homepage — wrong confidence level on scouted results. | Any funder URL with path `/en` (common on multilingual sites) gets `confidence: medium` instead of `low` — affects all scouted results | Tightly coupled to confidence-scoring block at line 433-436 and genericLink flag | Modifies scoutResults state indirectly through confidence assignment in aiScout | None | Remove local definition; import `isHomepageOnly` from `../utils` |
| **src/components/ScoutPanel.jsx** | :373-480 | `aiScout` is ~108 lines spanning three concerns: prompt construction + dual API calls, dedup + confidence scoring, async URL verification. `setScouting(false)` at line 455 is marked done before async side-effects complete. Bare `catch {}` at line 478 silently swallows URL verification errors leaving `urlStatus` as null on all results. Second write to `scoutResults` inside URL verification can race with user interactions. | High — errors in URL verification block silently fail; state ordering bugs on future edits | Calls normaliseFunder, titlesSimilar, calcScoutFitScore, isHomepageOnly, isGroundingRedirect, isUsableUrl, verifyUrls, generateScoutBrief, scoutPrompt, parseScoutResults, SCOUT_FALLBACK | Yes — writes scoutResults, scoutSort; calls setScouting twice | None | Extract `fetchScoutResults(promptArgs, scoutMarket)` and `scoreAndDedupe(allParsed, existing, rejectedFunders)` helpers; add comment that setScouting(false) before URL verification is intentional; fix bare catch to at least log |
| **src/components/ScoutPanel.jsx** | :393-407 | `scoutMarket === "both"` fires two sequential API calls separated by a hard-coded `await new Promise(r => setTimeout(r, 4500))`. Comment says this was a Gemini RPM rate-limit workaround. Backend now proxies to OpenAI. The 4.5-second delay is dead configuration — every 'both' scout run is unnecessarily 4.5s slower. | UX — every 'both' scout is 4.5s slower than necessary | Backend server/routes/ai.js rate-limit behaviour | No | None | Confirm whether OpenAI web-search calls require any sequential delay; if not, remove setTimeout and run both calls concurrently via Promise.all |
| **src/components/ScoutPanel.jsx** | :482-503 | `addScoutToPipeline` `market` derivation falls back to `"sa"` for any non-International type. Types like 'Development Agency' or 'Impact Investor' can be either SA or global — the fallback may silently mis-tag them. | Medium — mis-tagged market affects filtering in Pipeline if market-based views exist | scoutPrompt returns market in each result object; fallback only matters for SCOUT_FALLBACK items which don't set market | Yes — creates new grant via onAddGrant → backend PUT | None | Review whether market fallback is correct for all SCOUT_TYPE_MAP values; default to `"sa"` only for explicitly SA types |
| **src/components/Vetting.jsx** | :82 | `approveGrant` sets `stage: "vetting"` instead of `stage: "qualifying"`. The entire purpose of the Vetting view is to advance grants from vetting to qualifying. Tooltip also reads 'Move to vetting stage' — consistent with wrong target. | Every single-approve action on a fully-vetted grant silently leaves it in vetting stage. Pipeline view, Dashboard qualifying count, gate enforcement all downstream of this write. | onUpdateGrant propagates to Supabase PUT; Pipeline.jsx and Dashboard.jsx read stage to bucket grants into columns | Yes — writes to persistent grant stage in Supabase | None | Change `{ stage: "vetting" }` to `{ stage: "qualifying" }`; update tooltip text on line 454 |
| **src/components/Vetting.jsx** | :105 | `bulkApprove` also sets `stage: "vetting"` — same wrong target as `approveGrant`. All bulk-approved grants stall in vetting with no user-visible error. | Same as approveGrant — all bulk-approved grants silently remain in wrong stage | Same as approveGrant | Yes — writes to persistent grant stage in Supabase | None | Change `{ stage: "vetting" }` to `{ stage: "qualifying" }` |
| **src/components/Funders.jsx** | :376 | Remove button shown when `(assignedOwnerId || relOwner)` — so it appears even for hardcoded defaults. Clicking Remove stores then immediately deletes the key; after reload, the hardcoded default reappears unchanged. User sees a Remove action that appears to succeed but has no lasting effect. | UX only — no data corruption, funderOwners KV not dirtied | Reads assignedOwnerId, relOwner; calls assignFunderOwner | Yes — funderOwners KV store | None | Gate Remove button on `!!assignedOwnerId` only; if intent is to suppress hardcoded defaults, store a `"none"` sentinel and handle in resolution logic at line 257-259 |
| **src/components/DocVault.jsx** | :135-154 | `changeDocCategory` has a dead stub: first `try` block imports `apiFetch` from '../api' and does nothing with it; catch is empty. Real work follows using raw `localStorage.getItem('gt_slug')` / `'gt_token'` reads instead of the api layer. Comment documents intent that was never completed. | Silent failure on non-OK server response with no user feedback; auth path bypasses centralised api layer | Tightly coupled to manual localStorage reads that duplicate the auth layer | Yes — mutates uploads state on success; silent failure leaves UI and server out of sync | None | Remove dead first try/catch block; replace raw localStorage reads with api layer's authenticated fetch wrapper (f() or named export) |
| **src/components/DocVault.jsx** | :405-429 | 25-line async function with try/catch, raw fetch, DOM mutation, and uploads state refresh embedded inside `filtered.map()` JSX render loop. Uses raw localStorage reads for auth. DOM mutation side-effects tangled with React state refresh. | Re-extract is user-visible feature on every document row lacking text; DOM mutation parallel with React makes interaction path fragile | Uses raw localStorage auth duplicating changeDocCategory auth pattern | Yes — mutates uploads state; also mutates DOM button directly | None | Extract to named `handleReextract(docId, e)` handler at same scope as handleUpload; use api layer instead of raw fetch |
| **src/components/AutoFillPanel.jsx** | :99-111 | `useEffect` that auto-triggers `runExtractRequiredDocs()` has incomplete dependency array — `requiredDocs`, `extractingDocs`, `onRunAI` are read inside the condition but not listed. If `onRunAI` changes identity, auto-extraction will not re-fire as intended. | Medium — affects auto-extraction of required documents on panel open; could silently skip extraction when prop identity changes | Reads grant.funderBrief, grant.aiResearch, requiredDocs, extractingDocs, onRunAI | Yes — triggers AI call + updates requiredDocs state; persists to grant via onUpdateGrant | None | Add `requiredDocs`, `extractingDocs`, `onRunAI` to dependency array; or move `runExtractRequiredDocs` above this effect so ESLint can see captured deps |
| **src/components/AutoFillPanel.jsx** | :671-694 and 706-719 | Manual URL input rendered in two separate JSX branches (no-URL case and has-URL case) sharing the same `manualUrl`/`showManualUrl` state and `saveManualUrl` handler. ~25 lines each duplicated with minor structural difference. Any change (validation, a11y, styling) must be applied twice and can drift. | Medium — any future change to input must be applied twice and can drift | manualUrl, showManualUrl, saveManualUrl, onUpdateGrant, grant.applyUrl | Yes — shares state; both branches write through same handlers | None | Extract local `ManualUrlInput` component accepting `onSave` and `onCancel` props; render from both branches |
| **src/components/Admin.jsx** | :83-92 | `AI_FIELDS` is a module-level constant listing AI fields, but the filter expression at lines 414 and 436 that decides which grants to reset does not include `aiRecommendedAsk` or `researchHistory` — both present in AI_FIELDS. The reset silently skips grants whose only AI content is a recommended ask or research history. Also, AI_FIELDS is invisible to callers in 5 other files that reference the same field names independently. | Every grant in the pipeline. Incorrect filter means reset silently skips grants with aiRecommendedAsk or researchHistory set. | The mismatch between AI_FIELDS keys and filter predicate — both live 22 lines apart | Yes — persistent Supabase mutation | None | Move AI_FIELDS to src/data/constants.js; fix filter expression to add `|| g.aiRecommendedAsk || g.researchHistory` |
| **src/components/Login.jsx** | :89-98 | URL-param reset-token handler reads `params.get('slug')` to validate but never uses `tokenSlug` — the component's `slug` prop is used in `submitResetPassword` instead. A user opening a reset link for org A while the app is scoped to org B attempts reset against wrong slug — fails at server with opaque error. | Auth reset path — fails silently with network error; confusing for user | resetPasswordWithToken in api.js uses Login's slug prop, not URL param | No — network error only | None | Validate `tokenSlug === slug` and show error if they differ; or use `tokenSlug` from URL as the slug passed to `resetPasswordWithToken` |
| **src/components/Settings.jsx** | :386,874 | Single `fileRef` attached via `ref={fileRef}` inside `docs.map()` loop is overwritten on every map iteration. Safe only because single-expand invariant is maintained. If expansion logic ever allows two panels open simultaneously, clicks on any upload button would trigger the last-rendered doc's file picker — silently uploading to wrong compliance record. | Compliance document upload could attach file to wrong org_doc record, corrupting compliance status shown on grant cards | Coupled to toggleExpand (line 476) and single-expand constraint | Yes — writes to compliance records via onUpsertCompDoc | None | Replace single fileRef with per-document approach: render `<input type=file>` inline with onChange handler; no ref needed — call handleUpload directly from onChange |
| **src/components/Settings.jsx** | :518-533 | `handleSaveMeta` is async, calls `await onUpsertCompDoc(...)`, has no try/catch. Rejection propagates silently — no user feedback. Save button has no disabled/loading state — user can click multiple times while a previous call is in-flight. | User believes expiry date/notes were saved when they were not; compliance records silently out of date | Same API path as handleUpload (which does have error handling) | Yes — persists compliance metadata to Supabase | None | Add try/catch; show inline error message; add local busy state for Save button to prevent double-submits |
| **src/components/docxGenerator.js** | :178-271 vs 537-612 | Cover-page build block copy-pasted verbatim between `generateDocx` and `generateDocxFromSections`. Any branding change must be applied in two places. | Both exported paths; any cover-page divergence produces different .docx output for the two call sites | Tightly coupled to meta destructuring and docxModule imports inside each function | Stateless — pure document construction | None | Extract `buildCoverPage(docxModule, { grantName, funder, orgName, date, ask, grantType })` helper returning `coverChildren[]`; call from both functions |
| **src/components/docxGenerator.js** | :305-343 vs 641-676 | `flushKV` closure defined identically inside both `generateDocx` and `generateDocxFromSections`. Any style change requires updating both closures. | Both export paths; divergence produces inconsistent KV table rendering | Depends on docxModule, colour constants, buildRuns, and outer contentChildren array | Stateless | None | Extract `buildKVTable(docxModule, kvBuffer)` returning a Table node; each function calls it in its flushKV and pushes result to contentChildren |
| **src/components/Calendar.jsx** | :176 | `EventChip` is a function component defined inside `Calendar`'s render body. React sees a new component type on every render — unmounts and remounts all EventChip subtrees. Month grid can render up to 126 chips; each render discards and recreates them entirely. | All three calendar views use EventChip. Every user interaction causes full DOM teardown and rebuild of all visible chips. | MonthView (line 248), WeekView (line 301) — both defined in same scope | No chip state, but event listeners re-attached on every mount | None | Hoist EventChip to module scope (outside Calendar); pass onSelectGrant as a prop |
| **src/components/Calendar.jsx** | :203 | `MonthView`, `WeekView`, `ListView` defined inside Calendar render body — same anti-pattern as EventChip. React creates new component types each render, causing full unmount/remount of active view on any Calendar state change. Focus, scroll position, and animation state are lost. | Entire visible calendar grid remounted on every Calendar state change | Each view closes over Calendar's memos (monthGrid, weekDays, eventsByDate, upcomingEvents, stageById, teamById) | Yes — ListView has inline grouped object built with no memoization | None | Hoist all three view components to module scope; pass dependencies as props; memoize grouped in hoisted ListView |
| **src/components/WelcomeTutorial.jsx** | :58-69 | `hasSeenTutorial` and `resetTutorial` exported but not imported anywhere. The component itself (`WelcomeTutorial`) is not imported anywhere in src/ including App.jsx. PRODUCT_CONTEXT.org lists it as Active but there is no import path wiring it into the running app — users never see the onboarding walkthrough. | Entire module is unreachable at runtime | Fully self-contained — localStorage only | Yes — localStorage key `ge_tutorial_seen_v1` | None | (a) Wire into App.jsx or shell component gated on `!hasSeenTutorial()` after login, OR (b) change PRODUCT_CONTEXT.org status to Deferred and add DEFERRED.org entry |
| **src/components/WelcomeTutorial.jsx** | :83 | Stale closure: keydown handler closes over `finish` (and transitively `onClose`) at render when `open` first becomes true. Effect dep array is `[open]`. If parent changes `onClose` while dialog is open, Escape key calls wrong callback — tutorial marks itself seen and closes but parent state not updated. | Single interaction path (Escape-to-close) — wrong callback if parent changes onClose during open session | Depends on parent's onClose prop being stable for lifetime of open session | Yes — localStorage write (markTutorialSeen) always correct; parent callback may be stale | None | Wrap `finish` in `useCallback` with `[onClose]` dep, or store onClose in a ref updated by a `useEffect([onClose])` and call `onCloseRef.current?.()` inside `finish` |
| **src/components/SectionCard.jsx** | :245-246 | `isError` detection uses only 3 prefix strings (`Error`, `Rate limit`, `Connection`) while canonical `isAIError` in utils.js covers 6. When server returns `Request failed with status 500`, `isError` is false and `hasText` is true — error string rendered as proposal content in view mode. User could copy/export/save corrupted content. | Any section generating a `Request failed`/`No response`/`The AI service` error will silently display the error string as proposal text | Must align with isAIError in utils.js and ProposalWorkspace.saveSectionEdit which correctly uses isAIError | Yes — affects hasText and isError controlling expanded state, status icon/colour, and view/error branch | utils.test.js covers isAIError exhaustively; SectionCard has no test file | Import `isAIError` from `../utils`; replace both line 245-246 with `const hasText = section?.text && !isAIError(section.text); const isError = section?.text && isAIError(section.text);` |
| **src/components/index.jsx** | :403-404 | `AICard.isError` checks against five prefixes using OR chaining not shared with any constant. `hasResult` at line 403 checks only three prefixes — `'Request failed...'` and `'The AI service...'` strings are classified as neither hasResult nor isError by `hasResult`'s check, only caught by `isError`. The asymmetry is fragile; a future prefix addition or reordering of checks could cause error strings to render as valid results. | Medium — AICard imported by GrantDetail (3 instances) and every view running AI actions | Coupled to error format emitted by server/routes/ai.js | Yes — expanded state affected if isError/hasResult are wrong | None | Extract shared `isAIError(result)` predicate covering all five prefixes; use for both `hasResult = !!result && !isAIError(result)` and `isError = !!result && isAIError(result)` |
| **src/__tests__/funderStrategy.test.js** | :196-203 | **TEST RED.** Asserts `funderStrategy({ funder:'Unknown Corp', type:'Corporate CSI', ... }).structure` contains `'B-BBEE Value Proposition'`. Root cause: `funderHasFullStructure` guard fires for the generic fallback `a` object because its default sections array has length >=4 and contains 'Budget'. Structure is assigned fallback sections instead of `structures['Corporate CSI']`. | Every funderStrategy call with an unrecognised funder name returns wrong structure — proposal scaffolding sent to OpenAI omits type-specific sections for all new-funder paths | funderStrategy.js:188 funderHasFullStructure heuristic | No | Currently RED | In funderStrategy.js line 188, add `!a.noIntel` to funderHasFullStructure condition so the generic fallback path falls through to `structures[t] || structures['Foundation']` |
| **src/__tests__/funderStrategy.test.js** | :211-215 | **TEST RED.** Asserts `funderStrategy({ funder:'Test', type:'Random Type', ... }).structure` contains `'The Challenge'` and `'Our Approach'` (Foundation fallback structure). Fails for the same root cause as the Corporate CSI test — same single-line fix resolves both. | Same as above | Same as above | No | Currently RED | Same fix as above — adding `!a.noIntel` to `funderHasFullStructure` resolves both failures simultaneously |

---

## Edge-of-Logical (flagged)

Logical but lower blast radius or better tested. Review before applying.

| Module | Location | Issue | Suggested Fix |
|---|---|---|---|
| **src/api.js** | :674-724 | `loadData` `useCallback` has empty dep array but calls `toast` and `dSave` inside. Both are stable today — no stale-closure bug — but missing deps make this a lint violation that will silently break if either becomes non-stable. | Add `toast` and `dSave` to `loadData`'s dependency array; both are stable so no runtime change |
| **src/api.js** | :826-882 | `updateGrant` closes over `stages` and `team` but is not wrapped in `useCallback`. Recreated on every render of AppInner — every child receiving `onUpdateGrant` re-renders whenever any AppInner state changes. | Wrap in `useCallback` with `[stages, team, dSave]` as deps; function uses functional form of setGrants so grants itself not needed |
| **src/App.jsx** | :1285-1290 | Silent error swallow in `onUpdateOrg`: if `apiUpdateOrg` throws, `setOrg` and `applyOrgTheme` are never called and user receives no feedback. Inline async handler has no try/catch. | Wrap in try/catch; call `toast(...)` on error (mirror pattern used in `upsertCompDoc` at line 935) |
| **src/hooks/useAI.js** | :204 | Magic number 50 used as loop cap for starred reference proposals but comment on line 196 says 'up to 3'. The budget arithmetic cap of `remaining - 200` is the real cap — the loop cap is effectively dead code that misleads about how many network round-trips can occur. | Change guard to `if (withText.length >= 3) break;` matching the comment; add inline note about budget constraint |
| **src/hooks/useAI.js** | :129 | `remaining` variable manual bookkeeping drifts from `maxCtx - orgCtx.length` if any block appends to `orgCtx` without decrementing `remaining`. Adding a new block before line 138 would silently over-fill the budget. | Replace manual `remaining` bookkeeping with `const remaining = () => maxCtx - orgCtx.length;` and use `remaining()` at every guard check |
| **src/hooks/useAI.js** | :273 | Outer `try` block wrapping all uploads/learnings/proposals context fetches has bare `catch {}` — silently swallows all errors including programming errors (TypeError from malformed cache write). | Change to `catch (e) { console.warn('[useAI] context assembly error:', e?.message); }` to preserve visibility while remaining non-blocking |
| **src/hooks/useAI.js** | :1363-1535 | `insights` and `strategy` handlers nested with confusing brace structure — inner `if (type === 'insights')` appears at same indent as its containing `if`, requiring comments to explain nesting. Closing braces annotated with comment labels to compensate. | Extract shared analytics loop into `buildPipelineStats(grants, stages, team)` helper; write two separate flat if-blocks for insights and strategy |
| **src/editLearner.js** | :209 | `mergeLearnings` error check is missing the `NO_STYLE_PREFERENCE` guard that `extractPreferences` (line 172) has. If merge AI call returns a response containing the literal `NO_STYLE_PREFERENCE`, it will be stored as a rule. | Add the same `NO_STYLE_PREFERENCE` guard to `mergeLearnings` error check, or centralise both guards in shared helper |
| **src/editLearner.js** | :28-30 | `countCharDiffs` samples only the first 500 characters. Intentional approximation or unfinished? Substantive mid-document changes below the 500-char mark can suppress background analysis for meaningful edits. | Either document the intentional approximation with a comment, or sample from beginning + middle + end for better coverage of longer texts |
| **src/editLearner.js** | :86-101 | `buildCompactDiff` uses greedy O(n*m) paragraph-matching loop with no early-exit. For proposals with many short paragraphs (n=20, m=20 = 400 similarity calls) this runs synchronously before the first `await`. | Not critical at typical proposal lengths (5-15 paragraphs), but worth a comment acknowledging synchronous cost. If proposals grow, wrap in `setTimeout(0)` or `queueMicrotask` to yield to renderer |
| **src/editLearner.js** | :110 | Diff output capped at 2500 chars but AI call uses 300-token budget. At ~4 chars/token, prompt overhead alone consumes ~200 tokens leaving ~100 tokens (~400 chars) for the diff. The 2500-char cap is over-generous; most of the diff is silently truncated by the model's context window. | Either raise `extractPreferences` token budget to 500-600, or lower the diff cap to ~800 chars |
| **src/editLearner.js** | :189 | `MAX_LEARNINGS_CHARS` (600) sliced at a byte boundary — can cut mid-line, producing a truncated final rule (e.g. `- Avoid passive voice in i`). Same issue on merge path (line 218). | Truncate by removing whole trailing lines: iterate lines, stop before exceeding MAX_LEARNINGS_CHARS |
| **src/data/prompts.js** | :62,67,78,97,99,101,102 | Year `2026` hardcoded in seven places across `systemByMarket` role/searchFocus strings and `searchScope` ternary. After 2026, scout calls will produce year-stale search directives degrading result quality with no error. | Extract `const SCOUT_YEAR = new Date().getFullYear();` at top of file; replace all literal `2026` occurrences with `${SCOUT_YEAR}` |
| **src/data/prompts.js** | :30-53 | `buildRejectionBlock` accesses `r.funder`, `r.focus`, `r.reasonText`, `r.reason`, `r.name` with no shape guard. A non-object element (string accidentally pushed into rejections) silently produces undefined values for all fields — misleading but empty rejection blocks injected into prompt. | Add filter at start: `const recent = (rejections || []).slice(-50).filter(r => r && typeof r === 'object');` |
| **src/data/prompts.js** | :105-137 | `system:` template literal in scoutPrompt assembles six distinct logical sections in a single 33-line unbroken string concatenation. Boundaries between sections invisible without careful reading. | Assign each logical section to a named const (contextSection, criticalInstructions, jsonSchema) and join with `[...].join('\n')` |
| **src/data/glossary.js** | :81-89 | B-BBEE (line 81) and BBBEE (line 86) are two separate GLOSSARY entries with identical definition strings. If both spellings appear in one proposal, both receive full definition bracketed inline — two identical expansions. Listed twice in appendix. | Merge into single GLOSSARY entry with combined regex: `/\bBBBEE\b|\bB-BBEE\b/i` |
| **src/data/glossary.js** | :221-224 | Idempotency guard `after.startsWith("(")` skips insertion whenever matched term is immediately followed by *any* opening parenthesis — including a naturally occurring parenthetical in the proposal text. First-use definition silently suppressed with no indication to reader. | Tighten check to verify text after `(` starts with first word of known definition; or explicitly document current behaviour in a comment |
| **src/components/Pipeline.jsx** | :56 | `CLOSED_STAGES` local constant duplicates information from constants.js STAGES. 'resubmit' is in STAGES but absent from CLOSED_STAGES — intent opaque. If a stage is added to STAGES it must also be classified here. | Add boolean field (`closed: true`) to each entry in STAGES constant in constants.js; derive CLOSED_STAGES from that |
| **src/components/Pipeline.jsx** | :498-604 | Unified filter bar rendered as IIFE inside JSX: recomputes openCount, dueSoonCount, awaitingCount, missedCount, and helper functions on every render without memoization. At ~100 grants with frequent re-renders (typing in search box), this is avoidable per-render cost. IIFE also makes render tree harder to navigate. | Extract into FilterBar sub-component or at minimum move counts into a useMemo; replace IIFE at line 1428 (person view) as well |
| **src/components/Pipeline.jsx** | :1290-1293 | Kanban card uses `['won','lost','deferred','archived'].includes(g.stage)` as inline literal — third source of truth for 'what is a closed stage'. Also calls `grantReadiness` twice per card per render with no memoization. | Replace literal array with CLOSED_STAGES; call `grantReadiness` once and bind result to a variable before return |
| **src/components/GrantDetail.jsx** | :2914 | 'Recently viewed' widget reads `g.viewLog` (line 2916) but the view-logging `useEffect` (line 427-436) writes to `g.log`. There is no write path that ever populates `g.viewLog`. Widget always renders nothing. Feature is silently broken for all users. | Either (a) change view-logging effect to write a dedicated `viewLog` array entry, or (b) remove the widget |
| **src/components/GrantDetail.jsx** | :785 | Engagement-mode switch logic duplicated in two IIFEs (lines 785-819 and 1204-1227) with identical `onUpdate` payloads and `Math.random()` for ID generation. If one is fixed, the other will diverge silently. | Extract single `toggleEngagementMode` function at component scope; use `uid()` (already imported) instead of `Math.random()` |
| **src/components/GrantDetail.jsx** | :427 | View-logging `useEffect` dep array `[grant?.id, currentMember?.id]` omits `onUpdate` and `grant.log`. `grant.log` is read inside the effect for deduplication — if it changes between effect runs, `alreadyLoggedToday` check uses stale log, potentially writing duplicate 'Opened grant' entries. | Add `onUpdate` and `grant?.log` to the dependency array |
| **src/components/GrantDetail.jsx** | :2521 | Outstanding-actions `SectionWrap` badge re-implements abbreviated version of `autoItems` count logic from `OutstandingActions.useMemo` but ignores `g.engagementMode`. Manual mode suppresses most auto items in the component but not in the badge — non-zero badge for items that don't appear in the list. | Move auto-item count logic into shared helper or expose from `OutstandingActions` so badge derives from same computed list |
| **src/components/GrantDetail.jsx** | :346 | `_pendingAI` effect's `runPending` duplicates research-fetch block from `runResearch`. The two copies differ: `runPending` calls `storeResearch` without `aiLog`. Silent omission in activity log for auto-triggered research. | In `runPending`, replace inline research-fetch block with call to `runResearch()` |
| **src/components/BudgetBuilder.jsx** | :49-59 | `useEffect` dep array is empty `[]` while effect reads `g`, `typeNum`, and `saved`. Stale-closure: auto-detect runs against initial `g` only. Latent rather than immediately broken because component appears to remount fresh per grant detail open — but suppresses lint warning and creates hidden contract that component is never reused across grants without unmounting. | Add `// eslint-disable-next-line react-hooks/exhaustive-deps` with explanatory comment for intentional run-once-on-mount pattern, OR refactor to run only when `g.id` changes: `useEffect(() => { ... }, [g.id])` |
| **src/components/BudgetBuilder.jsx** | :52-54 | `detectType` returns a PTYPE value object; code does reference-equality search over PTYPES to recover the key. Works because PTYPES values are module-level constants (same reference). If PTYPES is ever rebuilt (spread/copied in a refactor), the find returns undefined silently and auto-detect stops working with no error. | Change `detectType` to return the numeric key (or `{ key, ptype }` pair) instead of raw value object, eliminating the reverse lookup |
| **src/components/ScoutPanel.jsx** | :656-843 | Result-card JSX block inside `scoutDisplay.map` is ~188 lines with 6+ levels of nesting mixing layout, badge rendering, access-channel logic, expanded-detail grid, and reject popover. | Extract `ScoutResultCard` component accepting the `s` result object plus `onAdd`, `onReject`, `onToggleExpand`, `isExpanded`, `isRejecting`, `rejectText`, `onRejectTextChange` props |
| **src/components/Vetting.jsx** | :21 | Filter state initialised with comment `"all" | "mine" | "unvetted" | "vetted"` implying a "mine" option, but useMemo filter logic has no branch for `filter === "mine"` and toolbar only renders ["all", "unvetted", "vetted"]. "mine" variant is undocumented in DEFERRED.org. | Either (a) add 'mine' filter branch using `g.owner === currentUserId` and expose button in toolbar, or (b) remove 'mine' from comment and confirm it was never shipped; if deferred, add note to DEFERRED.org |
| **src/components/Funders.jsx** | :242-491 | `.map` callback for funder cards is ~250 lines with 10+ local variable declarations before JSX begins at line 296. All card behaviour requires scrolling through the entire block. | Extract card body into `FunderCard` component receiving fd, stages, funderOwners, expandedFunder, setExpandedFunder, assignFunderOwner, onSelectGrant as props |
| **src/components/Funders.jsx** | :348-357 | Dropdown portal position computed in a `ref` callback that fires once at mount. If the page scrolls between the key being set and the dropdown div mounting, `getBoundingClientRect()` reads a stale button position. Cross-browser stacking context edge cases possible with `position: fixed` inside grid card. | Use a `useLayoutEffect` or `ResizeObserver`-based portal approach; or document limitation with a comment since scroll-close listener already eliminates worst case |
| **src/components/Dashboard.jsx** | :293-294 | Win factors (`g.on`) parsed as comma-separated string; loss factors (`g.of`) parsed as array with `.flat()`. If a grant has `g.on` as an array (possible from migration or AI-set data), win-factor split would silently produce wrong output. | Normalise both fields: `const onFacs = Array.isArray(g.on) ? g.on : (g.on || "").split(",").map(s => s.trim()).filter(Boolean)` |
| **src/components/Dashboard.jsx** | :604-689 | In Play section rendered as 85-line IIFE in JSX containing non-trivial grouping logic (lines 605-619) plus full rendering tree. Grouping logic runs per-render without memoization. | Extract into named sub-component `InPlaySection({ pipe, stages, teamById, onSelectGrant })` or hoist grouping logic into separate useMemo before return |
| **src/components/Dashboard.jsx** | :441-788 | `Dashboard` function is ~780 lines with three large useMemo blocks, two event helpers, more useMemos, and a return with seven conceptual sections. `pipe` and `ana` memo bodies mix aggregation with derived-value construction in a single pass. | Immediate win: extract `ana` memo body into standalone pure `computeAnalytics(grants, team)` function for readability and testability; `funders` memo is a strong candidate too |
| **src/components/Admin.jsx** | :302-303 | Dead render: `<div>` at line 302 conditionally styles on `m.password_hash !== undefined` but API comment confirms `password_hash` is stripped — always `undefined`. The div produces no visible output. | Either remove the empty div, or render a visible indicator using `m.hasPassword` (field the API actually sends, confirmed at orgs.js:138) |
| **src/components/Admin.jsx** | :409-433 | Inline async handler on 'Yes, reset' button is ~25 lines of logic (filtering, iterating, per-grant save, progress tracking, state flush, error branch) embedded inside JSX. The only action handler not extracted to a named function — asymmetry makes it hard to test or read independently. | Extract to `const handleResetAI = async () => { ... }` alongside other handlers; replace onClick with `onClick={handleResetAI}` |
| **src/components/Admin.jsx** | :121-135 | `loadData` `setLoading(false)` placed outside try/catch — correct at runtime but placement is subtle. If an early return is ever introduced, the loading state could be leaked. | Move `setLoading(false)` into a `finally` block to make intent explicit |
| **src/components/Login.jsx** | :152-167 | `submitResetPassword` has `setBusy(false)` only reachable on the catch path. If the try-block succeeds, `setBusy(false)` is not called before `window.location.href` redirect — spinner state leaked if redirect is ever blocked (e.g., during testing or if navigation is intercepted). Also `const data =` at line 159 is assigned but never read. | Move `setBusy(false)` into a `finally` block; drop unused `data` variable |
| **src/components/Login.jsx** | :78 | Step state machine has six values but only a comment enumerates them. `pw`/`pw2` state not reset between transitions into 'setup' or 'forgot' steps — shared across steps with no reset. | Document the six-step FSM above the state declaration as a block comment; confirm `pickMember` clearing `pw`/`pw2` (it does on lines 119-120) is sufficient |
| **src/components/Login.jsx** | :110-115 | `maskEmail` shows both first and last characters for 2-char local parts ('ab' → 'a***b'), leaking the full address. Not a security defect (emails masked server-side before client) but a minor privacy/UX issue. | Only append last character when `local.length >= 4`: `local.length >= 4 ? local[local.length - 1] : ''` |
| **src/components/Login.jsx** | :89-98 | `tokenSlug` read from URL params but never used — component's `slug` prop used in `submitResetPassword` instead. Mismatch between URL slug and prop slug fails at server with opaque error. | Validate `tokenSlug === slug` and show error if they differ; or use `tokenSlug` from URL as the slug passed to `resetPasswordWithToken` |
| **src/components/OrgSelector.jsx** | :299 | Uncontrolled input read via DOM traversal `e.target.parentElement.querySelector('input')`. Couples button handler to DOM structure — silently reads null or wrong input if layout changes. Inconsistent with controlled-input pattern used for the identical field at line 415. | Add dedicated state variable `adminKeyInput` and wire `value`/`onChange` to it |
| **src/components/OrgSelector.jsx** | :73 | `autoSlug` guard compares slug state against the *old* `name` prop to derive the 'auto slug', while new value `n` is used to produce the updated slug. Under React's synchronous setState the comparison is reliable today but semantically wrong and fragile to future batching changes. | Derive old slug from old name before overwriting: `const oldDerived = name.toLowerCase()...`; guard becomes `if (!slug || slug === oldDerived)` |
| **src/components/OrgSelector.jsx** | :79-86 | Admin-key validation split across `handleCreateClick` (line 82) and the disabled Btn (line 421). The runtime guard is necessary because form `onSubmit` fires on Enter even when button is disabled — but the duplication is non-obvious. | Add comment in `handleCreateClick` explaining why guard exists independently of disabled state; or move to single validation function called from both |
| **src/components/Calendar.jsx** | :26 | VALARM block is a single array element with embedded `\r\n` sequences. A reformatter or future edit can silently produce malformed ICS. | Split the VALARM string into five separate array entries: "BEGIN:VALARM", "TRIGGER:-P1D", "ACTION:DISPLAY", "DESCRIPTION:Grant deadline tomorrow", "END:VALARM" |
| **src/components/Calendar.jsx** | :88 | `today` is `new Date()` in component body (not memoized). New object created on every render — `upcomingEvents` memo lists `today` as explicit dep; new Date() on every render breaks that memo. | Replace with `const today = useMemo(() => new Date(), [])` or compute `todayStr` string once and use as memo dependency |
| **src/components/Calendar.jsx** | :462-463 | 'Subscribe to calendar' click handler: if `slug` from `getAuth()` is null (expired session), `feedUrl` is constructed as `/api/org/null/calendar.ics` and silently copied/prompted to the user. | Add guard: `const { slug } = getAuth(); if (!slug) return;` before constructing feedUrl, or display user-visible error |
| **src/components/SectionCard.jsx** | :441-450 | History restore index inversion is implicit and fragile. Array displayed reversed; restore call translates back via `section.history.length - 1 - i`. Both sides must stay in sync — a future change reversing only one side silently restores the wrong version. | Extract reversed display into named variable `const displayHistory = section.history.slice().reverse()`; document the inversion with a comment |
| **src/components/SectionCard.jsx** | :49 | `new RegExp(STAT_PATTERN.source, 'g')` constructed on every line matching `STAT_LINE_PATTERN`. Recreation is correct (module-level STAT_PATTERN is stateful with the `g` flag) but non-obvious — a future reader may "fix" it by reusing the module-level regex and introduce a stateful iteration bug. | Add comment: `// Must create a fresh regex each call — module-level STAT_PATTERN is stateful (lastIndex) with the g flag.` |
| **src/components/index.jsx** | :96 | `Timeline`'s `getMember` helper evaluates `(team || []).find(...)` twice per call and is recreated on every render (closure captures `team`). `Row` inner component also defined in Timeline body — new reference every Timeline render. With many grants the chained `find` over `team` runs O(grants × team-size) per render. | Build a `teamById` Map once before Row/Band definitions; getMember becomes `teamById[id] || teamById['team'] || DEFAULT_MEMBER` |
| **src/components/index.jsx** | :121-148 | `Band` component defined inside `Timeline`'s function body. Secretly closes over `showAllComing` and `setShowAllComing` from outer scope. `truncate` prop appears to control show-more button but the actual expanded/collapsed state is read from closure — misleading prop API. | Either pass `showAll` and `onToggleAll` as explicit props to Band; or inline the truncate/show-more logic directly into the 'Coming Up' Band call |
| **src/__tests__/funderStrategy.test.js** | :38-42 | Test titled 'treats empty/null as matching (substring edge case)' asserts `isFunderReturning('')` and `isFunderReturning(null)` both return `true` — documents odd behaviour as correct rather than flagging it as a bug needing a fix. The test should be updated alongside the fix to isFunderReturning. | After adding `if (!n) return false;` guard to `isFunderReturning`, update test expectation to `toBe(false)` for both empty string and null |
| **src/__tests__/funderStrategy.test.js** | :109-125 | `multiCohortInfo` tested for `'3 x type 2 cohorts'` and `'2 cohorts in 2026'` but not for `'2 cohorts type 3'` (order-reversed pattern). Regex would silently return `typeNum:1` for `'2 cohorts type 3'` because second branch matches first via `.match` fallback. Coverage gap. | Add test case: `expect(multiCohortInfo({ notes: '2 cohorts type 3' })).toEqual({ count: 2, typeNum: 3 })` — this will currently fail |
| **src/__tests__/utils.test.js** | :143-163 | `isAIError` coverage gap: no test covers mid-string pattern (e.g. `'Unexpected rate limit exceeded'` returns false), case sensitivity, or the boundary between error-looking and valid content. The startsWith contract is unverified. | Add negative-boundary test: `expect(isAIError('Unexpected rate limit exceeded')).toBe(false)` to document that mid-string patterns do not match |
| **src/__tests__/utils.test.js** | :53-92 | `cleanProposalText` tests do not cover the `applyGlossary` integration (step 5). All test inputs are free of GLOSSARY terms — applyGlossary is a no-op in every test case. If applyGlossary throws or corrupts text, no test catches it. | Add one test with text containing a known glossary term (e.g. `'The SETA levy funds this cohort.'`) and assert output contains the bracketed definition |
| **src/__tests__/utils.test.js** | :28-33 | Fenced-JSON test exercises ` ```json ` variant only. Implementation also matches ` ``` ` (no language tag) per utils.js:31. That variant is not exercised. | Add sibling test case with plain ` ``` ` (no 'json' tag) to verify the optional-language branch |
| **src/__tests__/utils.test.js** | :1-193 | Thirteen exported functions from utils.js have zero test coverage: `isGroundingRedirect`, `isUsableUrl`, `isHomepageOnly`, `normaliseFunder`, `grantCompleteness`, `sanitizeNotes`, `deadlineCtx`, `readabilityScore`, `readabilityLabel`, `detectSubmissionMethod`, `detectUnsolicited`, date helpers `td`/`addD`. | Prioritise: `deadlineCtx` (POST_SUBMISSION vs PRE_DRAFT branching), `readabilityScore` (Flesch formula sanity-check), `sanitizeNotes` (Type N sentence removal). Lower priority: `isGroundingRedirect`, `isUsableUrl`, `normaliseFunder` |
| **src/theme.js** | :29/47/83 | `C.accent`, `C.amber`, `C.yellow` all hold identical initial value (`#C17817`); `applyOrgTheme()` keeps all three in sync. No documented semantic difference — consumers choosing between them have no signal about which to prefer; future divergence would be accidental. | Add comment at declaration noting amber/yellow are backward-compat aliases for accent and should not be used in new code; long-term migrate usages to C.accent |

---

## Auto-Fixable Mechanical (dry-run, NOT applied)

Safe mechanical cleanups. Apply first, under the human gate, before touching logical fixes.

| Module | Location | Issue | Suggested Fix |
|---|---|---|---|
| **src/api.js** | :249-253 | `uploadOrgLogo` dead `!res.ok` check after `f()` — unreachable because `f()` already throws on non-OK | Remove the `if (!res.ok)` block at lines 249-253 |
| **src/api.js** | :473-477 | `uploadFile` same unreachable `!res.ok` pattern | Remove the `if (!res.ok)` block at lines 473-477 |
| **src/api.js** | :484-489 | `addYouTubeUrl` same unreachable `!res.ok` pattern | Remove the `if (!res.ok)` block at lines 484-489 |
| **src/api.js** | :347-363 | `updateComplianceDoc` and `createComplianceDoc` pass explicit `Content-Type: application/json` header that `f()` already sets for all non-FormData bodies | Remove `headers: { 'Content-Type': 'application/json' }` from both compliance function calls |
| **src/utils.js** | :210 | `const stripCode = text;` is a no-op identity assignment — name implies stripping that does not occur | Rename `stripCode` to `doc` (or use `text` directly) so name does not imply a transformation that does not occur |
| **src/utils.js** | :45 | Comment attributes Gemini behaviour; app was migrated to OpenAI (commit 51ceb3b). Comment on `isGroundingRedirect` also stale. | Update comment on line 45 to 'The AI model sometimes uses these...'; for `isGroundingRedirect`, add note it is retained for backward-compatibility with persisted data from before migration |
| **src/utils.js** | :87 | Step numbering inconsistency: `// 5. Phase 6:` mixes two numbering schemes | Change `// 5. Phase 6:` to `// 5.` |
| **src/utils.js** | :323 | LOI regex `/\b(letter of inquiry|\bloi\b)\b/` has redundant `\b` on `loi` sub-pattern inside alternation that already has outer `\b` | Simplify to `/\b(letter of inquiry|loi)\b/` |
| **src/App.jsx** | :219-220 | Dead imports: `dlabLogo` (dlab.png) and `geIcon` (ge-icon.png) imported but never referenced | Remove both import lines |
| **src/App.jsx** | :2 | Dead import: `MONO` imported from `./theme` but never used in App.jsx | Remove `MONO` from the theme import destructuring |
| **src/App.jsx** | :3 | Dead import: `effectiveAsk` imported from `./utils` but never called in App.jsx | Remove `effectiveAsk` from the utils import destructuring |
| **src/App.jsx** | :14 | Duplicate module import: `Component` imported from `react` on line 14; first React import is on line 1 | Add `Component` to existing line-1 React import and delete line 14 |
| **src/App.jsx** | :1278-1284 | Dynamic import of already-statically-imported module: `onUpdateProfile` uses `await import('./api')` to get `updateProfile` which is not in the named static import list at lines 5-11 | Add `updateProfile as apiUpdateProfile` to static import; replace dynamic import with direct call |
| **src/App.jsx** | :302-314 | URL query string parsed twice: `orgSlug` initial state and `resetParams` each construct `new URLSearchParams(window.location.search)` | Extract `const params = new URLSearchParams(window.location.search)` before `useState` calls and reuse in both initialisers |
| **src/hooks/useAI.js** | :1277 | `_DEAD_findApplyUrl_legacy` branch is fully executable dead code with a working `return await api(...)` body. Type string is the only guard. Successor `findApplyUrl` (line 1231) is already present and active. No DEFERRED.org entry. | Delete lines 1277-1320 |
| **src/hooks/useAI.js** | :571-579 | Section-type classification flags (`isCover`, `isExecSummary`, `isBudget`, etc.) duplicated at lines 571-580 and re-declared again at lines 712-722 inside the `else` branch. Both sets are in scope inside the else block. | Remove re-declarations at lines 713-722; add inner-only flags (`isOrgBackground`, `isRegulatory`, etc.) to the outer declaration block |
| **src/data/prompts.js** | :4 | Header comment incorrectly claims `scoutBriefPrompt` is used by server/jobs/scout.js; it is exclusively used in ScoutPanel.jsx | Change comment to: '`scoutPrompt` is used by Pipeline/ScoutPanel and server/jobs/scout.js. `scoutBriefPrompt` is used by ScoutPanel only.' |
| **src/data/prompts.js** | :96-102 | `searchScope` nested inline ternary spanning 7 lines with unreachable fallback 'both' branch in practice | Extract named helper `buildSearchScope(market, keywords)` using if/else-if block |
| **src/data/prompts.js** | :30 | `buildRejectionBlock` exported but has no external caller — only `scoutPrompt` in same file (line 89) uses it | Remove the `export` keyword |
| **src/data/glossary.js** | :181 | Spurious `regex.lastIndex = 0` reset in `detectGlossaryTerms`. None of the GLOSSARY entries carry a `g` or `y` flag — `lastIndex` is always 0; the reset and its comment are misleading no-ops | Remove the `regex.lastIndex = 0;` line and its comment |
| **src/data/glossary.js** | :215-217 | Misleading comment in `applyGlossary`. Line 215 says '(must be non-global)' but lines 216-217 immediately derive a global version from it. The actual invariant is not stated. | Replace comment with: 'Build a fresh global regex from the stored (non-global) source so exec can scan starting at lastIndex 0 without mutating the shared GLOSSARY entry.' |
| **src/data/constants.js** | :108-115 | `PERSONAS` exported and commented as 'LEGACY fallback / Active app uses team data from DB'. Never imported anywhere in the live application. Not listed in DEFERRED.org. | Add PERSONAS to DEFERRED.org under Dormant Features with reactivation condition, OR remove the export if never to be reactivated |
| **src/data/constants.js** | :Pipeline.jsx:56, Archive.jsx:12, ScoutPanel.jsx:246 | `CLOSED_STAGES = ['won', 'lost', 'deferred', 'archived']` defined identically in three separate files as file-local constant. If a new terminal stage is added to STAGES, all three copies must be updated independently. | Export CLOSED_STAGES from constants.js and import it in the three consuming files |
| **src/data/funderStrategy.js** | :63-66 | `selectOptimalBudget()` duplicates the notes-based type-scan loop already in `detectType()`. Lines 63-66 re-implement `for (let i = 8; i >= 1; i--) ... n.includes('type N')` identically, creating a second copy of the pattern-matching logic that can drift. | Replace duplicated loop with `const detected = detectType(g)` then derive typeNum with explicit null-guard |
| **src/data/funderStrategy.js** | :117-166 | ~60-entry `angles` data literal inlined inside `funderStrategy()` function body — constructed on every call. Reader must scroll through ~50 lines of domain data before reaching the logic at lines 168-190. | Hoist `angles` to module-level const (as FUNDER_ANGLES or similar) outside `funderStrategy()` |
| **src/components/Pipeline.jsx** | :1340 | `closedDaysAgo` assigned (computed via O(n) log scan) but never read or rendered | Remove the line |
| **src/components/Pipeline.jsx** | :395 | `closedStages` computed but never consumed anywhere in the file | Remove the declaration |
| **src/components/Pipeline.jsx** | :55 | `VIEW_OPTIONS` constant declared but never used — view selector uses hardcoded inline `<option>` literals | Either remove VIEW_OPTIONS or use it to drive the select |
| **src/components/Pipeline.jsx** | :774 | `Btn onClick` uses `alert()` for parse errors while the identical Enter-key path uses `onToast` | Replace `alert()` with `onToast?.('Could not parse grant from URL. Try adding manually.', { type: 'error' })` |
| **src/components/GrantDetail.jsx** | :1046 | `false &&` permanently disables 'Apply' link inside About card — renders nothing, but dead block ships in bundle. Comment confirms Phase 3 removal was intentional; block was never deleted. | Delete lines 1046-1051 |
| **src/components/GrantDetail.jsx** | :1723 | `rollTheDice` is a 70-line async function recreated on every render inside a render-time IIFE. Captures many closure variables. Passed to button `onClick` — button always re-renders. | Lift `rollTheDice` to component scope (above the `return`) and wrap with `useCallback` with its dependencies listed |
| **src/components/GrantDetail.jsx** | :392 | `storeResearch` is a plain function in the render body (not `useCallback`). Called from `runResearch` (`useCallback`-wrapped with `[grant, onRunAI, onUpdate]`) and from `runPending` `useEffect`. Because `storeResearch` has new identity on every render, `runResearch`'s `useCallback` dep array would need to include it but does not — `runResearch` closes over stale `storeResearch` after any re-render. | Wrap `storeResearch` in `useCallback` with `[onUpdate]` deps; add `storeResearch` to `runResearch`'s dependency array |
| **src/components/GrantDetail.jsx** | :566 | `grantReadiness(g, complianceDocs)` called three times per render (sidebar IIFE, sticky-bar IIFE, Readiness Bar) with no memoization. Called on every render including fast state updates like keystroke in a textarea. | Compute `const readiness = useMemo(() => { try { return grantReadiness(g, complianceDocs); } catch { return null; } }, [g, complianceDocs])` once at component scope; use in all three render sites |
| **src/components/ProposalWorkspace.jsx** | :6 | Unused import: `detectType` imported from `../data/funderStrategy` but never referenced | Remove `detectType` from the named import on line 6 |
| **src/components/ProposalWorkspace.jsx** | :383-424 | Misleading variable name `remaining` in `migrateToSections` — declared as full text and never mutated, but name implies progressive consumption | Rename `remaining` to `fullText` |
| **src/components/ProposalWorkspace.jsx** | :460-474 | Readability badge computed with inline IIFE inside JSX. `assembledText &&` short-circuit is redundant since it is always a string. | Compute `readabilityBadgeProps` as a derived value above the return statement; remove IIFE from JSX |
| **src/components/ProposalWorkspace.jsx** | :503-524 | Glossary toggle button rendered via IIFE. `buildGlossaryAppendix` called here (line 503) and earlier on line 376 — called twice on same base text on every render reaching this branch. | Compute `const glossaryAppendix` once above the return; reuse in both `assembledText` IIFE and glossary button rendering |
| **src/components/BudgetBuilder.jsx** | :100-105 | `hasChanges` memo: `(saved.years || 1)` fallback in the comparison is duplicated from the useState initialiser (`saved?.years || 1`). If the saved schema drops the `years` field default inconsistently, the two `|| 1` fallbacks must stay in sync. | Extract `const savedYears = saved?.years ?? 1;` before useState; compare directly without inline fallback |
| **src/components/BudgetBuilder.jsx** | :565-566 | Cancel button (Edit→revert) reconstructs state by directly reading `saved.*` fields inline. Duplicates same defaulting logic as useState initialisers. If a new field is added and its initialiser is updated, Cancel handler must also be updated manually. | Extract `resetToSaved()` function setting all five state atoms from `saved`; call from Cancel button and any future reset paths |
| **src/components/BudgetBuilder.jsx** | :30-31 | `TYPE_OPTIONS` is module-level constant derived from PTYPES. Type 6 (Cyborg Habits) has `students: null, cost: null` which are handled correctly at line 258 with guards. No issue — noting for completeness. | No action required |
| **src/components/BudgetBuilder.jsx** | :158 | `addItem` uses `parseInt(newAmount) || 0` (string, lazy-normalise); `updateItem` uses `parseInt(e.target.value) || 0` (number, immediate normalise). Different representations mid-flight before normalisation — minor inconsistency. | Normalise `newAmount` to number on change: `setNewAmount(parseInt(e.target.value) || 0)` to match updateItem path |
| **src/components/ScoutPanel.jsx** | :376-381 | `existing` array and `existingNormalised` set built with two separate `.filter(…).map(…)` passes over `grants`. First filter result discarded; second duplicates it. | Compute both in one pass: `const activeGrants = grants.filter(...); const existing = [...new Set(activeGrants.map(...))]; const existingNormalised = new Set(activeGrants.map(normaliseFunder));` |
| **src/components/ScoutPanel.jsx** | :268-282 | `useEffect` that loads the scout brief from KV defines local `errRe` inline while `generateScoutBrief` at line 326 defines functionally equivalent `isApiErrorString` separately. Same error-detection pattern in two places. | Use already-defined `isApiErrorString` in the `useEffect`; move its definition above the effect or hoist to module scope; remove local `errRe` |
| **src/components/ScoutPanel.jsx** | :195-196 | `const normaliseFunder = utilNormaliseFunder;` is an unnecessary local alias | Change import on line 3 to `import { ..., normaliseFunder } from "../utils";` and remove line 196 |
| **src/components/ScoutPanel.jsx** | :311-313 | `scoutDisplay` sorted by `scoutSort` then unconditionally re-sorted by `rejected` at line 315 — two O(n log n) passes per render. | Combine into one comparator: primary key = `rejected` flag (0/1 ascending), secondary key = selected sort criterion |
| **src/components/Vetting.jsx** | :321-437 | Four vetting-check buttons are four near-identical JSX blocks (~15-property inline style, aria-pressed/label pattern, icon swap), differing only in `checkKey`. ~90 lines of duplication. | Extract `VettingCheckButton(grantId, checkKey, checked, label, icon, onToggle, children)` component; each row becomes `<VettingCheckButton ...>` |
| **src/components/Vetting.jsx** | :85-90 | `rejectGrant` takes `grantId` then re-fetches grant via `grants.find()` to read `g.log`. Caller at line 471 already has `g` in scope. Redundant linear search on every rejection. | Change signature to `rejectGrant(grant, reason)`, use `grant.log` directly, update callers to pass grant object |
| **src/components/Vetting.jsx** | :388 | `daysLeft < 14` evaluated without null guard (null < 14 is false in JS — falls to C.t2 silently). Inconsistency with the earlier `daysLeft !== null &&` pattern. | Add explicit null guard: `daysLeft !== null && daysLeft < 0 ? C.red : daysLeft !== null && daysLeft < 14 ? C.amber : C.t2` |
| **src/components/Funders.jsx** | :95 | Three props destructured but never consumed: `team`, `onNavigate`, `onLaunchTour`. Component imports and uses `TEAM` directly from constants. | Remove `team`, `onNavigate`, `onLaunchTour` from destructured props if AI-research/tour entrypoints remain absent; if planned, document in DEFERRED.org |
| **src/components/Funders.jsx** | :105 | `data.value || data` guard reads a `{ value: ... }` envelope that the current server never sends. `kvGet` returns stored JSON directly. | Replace `data.value || data` with just `data` (after asserting `typeof data === 'object'`); remove dead branch or add clarifying comment |
| **src/components/Funders.jsx** | :184 | `FTYPE_COLORS` defined inside render function body — recreated on every render; referenced in three places in JSX | Hoist `FTYPE_COLORS` to module scope alongside `REL_OWNERS` |
| **src/components/Dashboard.jsx** | :154 | `onLaunchTour` accepted in prop destructuring but never referenced in component body or JSX | Remove `onLaunchTour` from destructuring if Dashboard intentionally has no tour entry point; or wire to a button |
| **src/components/Dashboard.jsx** | :226-229 | `ana` useMemo builds a `teamById` Map from `team` prop. An identical useMemo produces `teamById` at line 407 for `urgentGrants` and In Play rendering. Two independent computations from the same input. | Hoist `teamById` memo above both `ana` and `urgentGrants`; pass it into `ana` useMemo via its dep array |
| **src/components/Dashboard.jsx** | :399-404 | `runAI` helper sets `busySetter(false)` after the try/catch block rather than in a finally clause. If `setter(r)` throws synchronously, `busySetter(false)` still runs but after an incorrect catch, making the idiom misleading. | Wrap `busySetter(false)` in a finally block |
| **src/components/Dashboard.jsx** | :700-763 | Upcoming Follow-ups section IIFE: `const now = new Date().toISOString().slice(0, 10)` at line 701 is computed but never used (daysUntil computed differently at line 707). | Remove the unused `const now = ...` line |
| **src/components/Dashboard.jsx** | :865 | `const maxAsk = funders[0]?.totalAsk || 1` computed inside `.map()` callback on every iteration despite being constant (funders sorted by totalAsk descending). | Hoist `const maxAsk = funders[0]?.totalAsk || 1` to before the `.map()` call, outside the callback |
| **src/components/Dashboard.jsx** | :765-780 | Comment at line 765 reads '4. AI TOOLS (Report, Insights, Strategy)' but section only renders the Quarterly Report AIBlock. Pipeline Insights and Strategic Recommendations render inside the Pipeline Intelligence section (lines 1182-1204). | Update comment to '4. AI TOOLS (Quarterly Report)' and add cross-reference note |
| **src/components/DocVault.jsx** | :101-108 | `complianceStatus` useMemo computes `uploaded` (a Set) and `compUploaded` (filtered array) that are never referenced in the returned object. Dead code in the memo body. | Remove `uploaded` and `compUploaded` declarations from the useMemo body |
| **src/components/DocVault.jsx** | :42 | `ROLE_LEVELS` defined inside component function body — new object allocated on every render. | Move `ROLE_LEVELS` to module scope (before the component), alongside DOC_CATEGORIES and ORG_DOC_CAT_MAP |
| **src/components/DocVault.jsx** | :156-165 vs 474-478 | Two separate delete paths exist. `handleDelete` (line 156) is the intended handler but inline delete button (line 474) calls `deleteUpload` and `alert` directly, bypassing it. Confirm prompt wording also differs. | Replace inline onClick on delete button (line 473-485) with `onClick={(e) => handleDelete(doc.id, e)}`; remove duplicate logic |
| **src/components/AutoFillPanel.jsx** | :450-454 | `autoFindTriggeredRef` declared but never read or mutated after the comment that the auto-trigger was removed. Dead ref. | Delete the `const autoFindTriggeredRef = useRef(false);` declaration and accompanying comment lines |
| **src/components/AutoFillPanel.jsx** | :521-525 | `grouped` object (highConf/medConf/lowConf) computed from `mappings` every render but never referenced in JSX or any function. Mappings are rendered by iterating over the `mappings` array directly at line 872. | Delete the `grouped` const (lines 521-525) |
| **src/components/AutoFillPanel.jsx** | :579 | `{true && ( ... )}` always-true conditional wrapper — comment confirms 'Full submission flow — always-on'. Wrapper adds noise without guarding anything. | Remove the `{true && ( ... )}` wrapper and render children directly |
| **src/components/AutoFillPanel.jsx** | :226-279 | `downloadAsPdf` defines `escapeHtml` and `renderPara` as inner closures on every call. Both are pure functions with no closure over component state. | Lift `escapeHtml` (line 238) and `renderPara` (lines 231-237) to module-level constants above the component |
| **src/components/AutoFillPanel.jsx** | :557-576 | Channel-header colour selector is an IIFE inside JSX reading `submission.method` three times in nested ternaries. | Replace with `METHOD_PALETTE` map constant at module scope keyed by `submission.method`; render `<div>` directly with `palette = METHOD_PALETTE[submission.method] ?? fallback` |
| **src/components/Admin.jsx** | :436 | Duplicate filter expression: `grants.filter(g => g.aiResearch || ...)` evaluated again inline for the button label, after already computed into `withAI` (line 414) inside the handler. | Hoist `withAI` to component scope (or derive with useMemo) and reference in both handler and button label |
| **src/components/Admin.jsx** | :308-334 | Three action buttons each reset the other two toggle states inline via separate `set*` calls — pattern repeated three times with slightly different combinations. Fourth action type would require updating all three existing onClick handlers. | Replace three separate ID state variables (`editId`, `resetId`, `confirmDelete`) with a single `activeAction: { id, mode } | null` state |
| **src/components/Login.jsx** | :231-233, 273-276, 312-314 | Back-arrow button in password, setup, and forgot steps duplicates the same inline style object three times. | Extract a `backButtonStyle` constant alongside `inputStyle` at module scope |
| **src/components/Login.jsx** | :253 | Password form Submit button passes both `onClick={submitPassword}` (fires on click) and is inside `<form onSubmit={submitPassword}>` (fires on submit). Redundant — linters will flag it. | Remove `onClick={submitPassword}` from the Btn; add `type="submit"` to it instead |
| **src/components/Login.jsx** | :377 | Same onClick/onSubmit duplication as line 253 on the "Reset Password" Btn. | Remove onClick, add `type="submit"` to the Btn |
| **src/components/Settings.jsx** | :399 | `gcalSyncing` state declared and initialized but never written to (`setGcalSyncing` never called) and never read. Dead state. | Remove the `const [gcalSyncing, setGcalSyncing] = useState(false)` declaration at line 399 |
| **src/components/Settings.jsx** | :380 | `onLaunchTour` received as a prop but never referenced in the component body. | Remove `onLaunchTour` from destructured props; or add tour-launch affordance that was presumably intended |
| **src/components/Settings.jsx** | :667-700 | Save Colours and Reset to Defaults buttons defined as large async inline arrow functions in JSX. Rest of file extracts handlers as named functions. | Extract to named `saveBrandColors` and `resetBrandColors` alongside other handlers above return statement |
| **src/components/Settings.jsx** | :608-624 | Logo upload onChange handler is ~12-line async function defined inline on input element. Inconsistent with compliance upload pattern (handleUpload). | Extract to named `handleLogoUpload` function alongside handleUpload and handleSaveMeta |
| **src/components/Settings.jsx** | :402-420 | `gcal` useEffect uses dynamic `import('../api')` inside a window message handler. All other API functions imported statically. All four gcal functions are already in `../api` and could be static imports. | Add `getGcalStatus`, `syncAllToGcal`, `disconnectGcal`, `getGcalAuthUrl` to static import at line 5; remove dynamic imports in handlers |
| **src/components/OrgSelector.jsx** | :358-359 | Delete-button hover handlers are identical — both set background to `C.redSoft` which is already the resting background. No hover effect is produced. | Either give `onMouseEnter` a darker tint (e.g. `C.red + '40'`) or remove both handlers if no hover state is desired |
| **src/components/OrgSelector.jsx** | :119-129 | `header` assigned as JSX expression inside component body rather than extracted as sub-component. Re-created on every render. Referenced in two separate render branches. Indentation on line 127 is inconsistent (8-space vs surrounding 2-space). | Extract as function component above OrgSelector (no props needed; geLogo is module-level); render `<OrgHeader />` in both branches; fix line-127 indentation |
| **src/components/OrgSelector.jsx** | :202-265 | `deleteModal` is a 60-line JSX block assigned to a const inside the render body, not a component. Cannot use hooks; re-created every render. | Extract as `DeleteConfirmModal({ target, confirmSlug, onConfirmChange, onConfirm, onCancel, deleting, err })` above OrgSelector |
| **src/components/Calendar.jsx** | :311-320 | Inside `ListView`, `grouped` object built with plain for-loop on every render. No memoization. Coupled to `upcomingEvents` which already has its own memo. | After hoisting ListView, wrap `grouped` computation in `useMemo(() => { ... }, [upcomingEvents])` |
| **src/components/Calendar.jsx** | :406-413 | Four derived stats (`deadlinesThisWeek`, `deadlinesThisMonth`, `overdueEvents`, `overdueCount`) computed unconditionally on every render via `filteredEvents.filter(...)` with `parseLocalDate` per event. | Wrap each stat (or group into one useMemo returning an object) with `useMemo(() => ..., [filteredEvents, todayStr])` |
| **src/components/Calendar.jsx** | :371-372 | `effectiveAsk(evt.grant)` called twice in same JSX expression — once for truthiness check and once for formatted value. | Assign `const ask = effectiveAsk(evt.grant)` at top of map callback; use `ask` in both places |
| **src/components/Calendar.jsx** | :135-145 | `monthGrid` while-loop has redundant `|| weeks.length < 6` condition and `if (weeks.length >= 6) break` inside loop. Loop always produces exactly 6 weeks via the break; the OR clause is dead. | Simplify while condition to `while (weeks.length < 6)` and remove the `if (weeks.length >= 6) break` inside the loop body |
| **src/components/WelcomeTutorial.jsx** | :94 | `finish` declared with `const` at line 94 — after the `if (!open) return null` early return at line 89 — but referenced at line 83 inside an effect defined at line 80. Works at runtime (effects run after render) but reverse textual order is a maintenance trap. | Move `finish` declaration to before the effects (above the `if (!open) return null` guard) |
| **src/components/WelcomeTutorial.jsx** | :121 | `C.primaryDark || C.primary` — `C.primaryDark` is always defined in theme.js (both statically and via dynamic setter). The `|| C.primary` fallback is unreachable. | Replace `${C.primaryDark || C.primary}` with `${C.primaryDark}` |
| **src/components/WelcomeTutorial.jsx** | :179 | Same dead fallback `C.primaryDark || C.primary` in Next/Get-started button gradient. | Replace `${C.primaryDark || C.primary}` with `${C.primaryDark}` |
| **src/components/SectionCard.jsx** | :244 | `total` prop destructured in function signature but never read anywhere in the component body. | Remove `total` from destructured props; if future 'Section N of M' display is planned, document in DEFERRED.org |
| **src/components/SectionCard.jsx** | :284-289 | `loadTitle` computation calls `name.toLowerCase()` six times in ternary chain without caching. | Hoist to `const nameLower = name.toLowerCase()` before the chain; replace all six calls |
| **src/components/SectionCard.jsx** | :503 | Empty-state Generate button calls `onGenerate()` with no argument (undefined) while header Regen button passes `instructions || undefined` and instructions-panel Regen passes `instructions`. Three inconsistent call shapes for the same function. | Normalise to `onGenerate(instructions || undefined)` consistently across all three call sites |
| **src/components/docxGenerator.js** | :424-496 vs 904-960 | Document assembly block (Document constructor, identical margin values, Header, Footer, Packer.toBlob, saveAs) duplicated in full across both functions. Page margin object appears four times. | Extract `buildDocument(docxModule, { orgName, grantName, coverChildren, contentChildren })` helper; extract margin constants to module-level variables |
| **src/components/docxGenerator.js** | :145 | JSDoc for `generateDocx` declares `meta.stage` as a parameter but `stage` is never destructured or read anywhere in the function body. | Remove 'stage' from JSDoc @param if intentionally unused; or destructure and use it (e.g. footer annotation) |
| **src/components/index.jsx** | :245 | `downloadDoc` encodes `<` as `String.fromCharCode(60)` to construct Blob HTML. Comment context shows this avoids linter/bundler warnings about inline HTML — no security benefit since content goes directly into a Blob and is never eval'd. | Replace `const lt = String.fromCharCode(60)` and `new RegExp(lt, "g")` with literal `'<'` and `/</g` regex |
| **src/components/index.jsx** | :17-41 | `DeadlineBadge`: guard `if (ctx && ctx.severity === 'ok' && d < 0) return null` sits below five derived assignments. Reader must scan all five lines before discovering the component can still return null. | Hoist the early-return to immediately after `const ctx = ...` is assigned, before the col/bg/pulse/label/icon derivations |
| **src/components/index.jsx** | :62-67 | `Sparkline` SVG circle `cx={(data.length - 1) / (data.length - 1) * w}` always reduces to `cx={w}`. The expression is a no-op simplification that reads as if it is using the loop variable formula. | Replace `cx={(data.length - 1) / (data.length - 1) * w}` with `cx={w}` |
| **src/components/index.jsx** | :279-317 | `AI_LOAD_STEPS` exported from this barrel but only consumed internally by `AILoadingPanel` (line 335). No external file imports it. | Remove the `export` keyword from `AI_LOAD_STEPS` |
| **src/__tests__/funderStrategy.test.js** | :147-152 | `selectOptimalBudget` test comment does not make the dependency on the internal preference-list ordering `[1,3,5,7,4,2]` explicit. | Update test comment to explicitly state the assumed preference ordering: `// Foundation preferred order [1,3,5,7,4,2]; Type 1 fits 4x within R2.5M` |

---

## quick_reference.org Accuracy

No `quick_reference.org` files exist for any module audited. Per `DEFERRED.org`, this is the documented expected state — per-module QR files are created as modules are documented, not at bootstrap, and only when a module gains 3+ files with non-obvious patterns. No inaccuracies to report; absence is intentional across all 21 modules.

The only editorial inaccuracy found is in `src/prompts.js`'s own file-header comment (lines 1-7), which incorrectly attributes `scoutBriefPrompt` to `server/jobs/scout.js` — that is captured as a flow-auto fix above.

---

## PRODUCT_CONTEXT Alignment

| Gap | Status | Note |
|---|---|---|
| `CLAUDE.md` instructs "Update org context: Edit src/data/context.js" | Inaccurate | context.js is not imported in the live app; live source is org_profiles DB record editable via Settings.jsx. Instruction produces no effect. |
| `ARCHITECTURE.org` line 36/56 state CTX/CTX_SLIM "injected into prompts" | Inaccurate | Live path reads from database (profile.context_slim); context.js is bypassed. |
| `PRODUCT_CONTEXT.org` lists WelcomeTutorial as Active | Inaccurate | Component not imported anywhere in src/; users never see the onboarding walkthrough. Update status to Deferred and add DEFERRED.org entry. |
| `_DEAD_findApplyUrl_legacy` block in useAI.js | No DEFERRED.org entry | Not listed as intentionally dormant; has no documented rationale. Should be deleted or documented. |
| `PERSONAS` export in constants.js | No DEFERRED.org entry | Commented as 'LEGACY fallback' but not in DEFERRED.org with reactivation condition. |
| Funder research AI web-search in Funders.jsx | Undocumented gap | PRODUCT_CONTEXT.org lists 'Funder research / intel' as Active but no AI call or navigation is present in Funders.jsx. The three unused props (onNavigate, onLaunchTour, team) are the only hooks for this flow. DEFERRED.org has no entry. |
| `CLOSED_STAGES` omits 'resubmit' | Undocumented | PRODUCT_CONTEXT.org does not document whether 'resubmit' is an active (non-closed) stage. The intent is implicit in the current code — it is treated as active — but not stated. |
| `WFAC`, `LFAC`, `TOPICS` in constants.js | Undocumented | Exported but only used in the intentionally-dormant monolith. Not in DEFERRED.org. |
| `qualifying->drafting` transition is ungated | Undocumented | PRODUCT_CONTEXT.org intentional decisions table does not cover this gap; it is not a protected decision. |
| `DEFAULT_STAGES` omits 'vetting' stage (App.jsx:261-275) | Undocumented | PRODUCT_CONTEXT.org intentional decisions table does not mention the DEFAULT_STAGES duplication or the vetting-stage omission. |
| ScoutPanel Gemini-era 4.5s delay (scoutMarket==="both") | Stale | PRODUCT_CONTEXT.org and CLAUDE.md both confirm backend now uses OpenAI; delay has no rationale in PRODUCT_CONTEXT.org or DEFERRED.org. |
| CLAUDE.md says App.jsx is "~2400 lines" and "has its own inline prompts" | Stale | App.jsx is 1306 lines; all AI calls route through `runAI` from `useAI` hook. The 2400-line figure referred to the pre-extraction monolith. `sB`/`sA` state helpers mentioned in CLAUDE.md appear only in the monolith reference file. |

---

## Appendix: Dropped on Lifecycle Grounds

The following items were moved here from the priority tables due to their lifecycle classification. Human: please sanity-check these calls.

| Module | Location | Issue | Lifecycle Tag | Note |
|---|---|---|---|---|
| src/App.jsx | :219-220 | `dlabLogo` and `geIcon` dead imports | Stale | Included in Auto-Fixable section above as flow-auto; lifecycle tag = Stale, not Intentionally Dormant — no ambiguity |
| src/App.jsx | :2 and :3 | Dead imports `MONO`, `effectiveAsk` | Stale | Same — included in Auto-Fixable |
| src/hooks/useAI.js | :1277 | `_DEAD_findApplyUrl_legacy` branch | Stale | No DEFERRED.org entry; no rationale for retention. Successor `findApplyUrl` is active. Included in Auto-Fixable. |
| src/data/constants.js | :108-115 | `PERSONAS` export | Stale | Commented 'LEGACY fallback' but not in DEFERRED.org. No live importer. Recommend adding to DEFERRED.org or removing. |
| src/data/constants.js | :19-21 | `WFAC`, `LFAC`, `TOPICS` exports | Stale | Only used in intentionally-dormant monolith. Recommend adding to DEFERRED.org if monolith-only, or wiring into live app if intended. |
| src/components/Pipeline.jsx | :1340 | `closedDaysAgo` dead variable | Stale | Included in Auto-Fixable |
| src/components/Pipeline.jsx | :395 | `closedStages` never consumed | Stale | Included in Auto-Fixable |
| src/components/Pipeline.jsx | :55 | `VIEW_OPTIONS` never used | Stale | Included in Auto-Fixable |
| src/components/GrantDetail.jsx | :1046 | `false &&` Apply link block | Stale | Phase 3 comment confirms intentional removal; dead block never deleted. Included in Auto-Fixable. Not Intentionally Dormant. |
| src/components/AutoFillPanel.jsx | :450-454 | `autoFindTriggeredRef` dead ref | Stale | Auto-trigger removed; ref left behind. Included in Auto-Fixable. |
| src/components/AutoFillPanel.jsx | :521-525 | `grouped` dead const | Stale | Never consumed. Included in Auto-Fixable. |
| src/components/AutoFillPanel.jsx | :579 | `{true && ...}` trivially-true conditional | Stale | Placeholder for removed feature flag. Included in Auto-Fixable. |
| src/components/Settings.jsx | :399 | `gcalSyncing` dead state | Stale | setGcalSyncing never called; gcalSyncing never read. Included in Auto-Fixable. |
| src/components/DocVault.jsx | :101-108 | `uploaded` and `compUploaded` dead variables in useMemo | Stale | Never referenced in returned object. Included in Auto-Fixable. |
| src/components/WelcomeTutorial.jsx | :58-69 | `hasSeenTutorial`, `resetTutorial`, component itself not imported | Stale | PRODUCT_CONTEXT.org lists as Active but there is no import path. **This is a critical routing gap, not a lifecycle drop** — listed here only to flag that the component has not been treated as Intentionally Dormant. See human-global table. |
| src/components/SectionCard.jsx | :244 | `total` prop never read | Stale | Included in Auto-Fixable |
| src/data/prompts.js | LEGACY exports | All exports except `scoutPrompt` and `scoutBriefPrompt` | Intentionally Dormant | Documented in both PRODUCT_CONTEXT.org and DEFERRED.org: 'Superseded by inline prompts in App.jsx; only scoutPrompt wired in. Reactivation path: re-import and replace the corresponding inline prompt when consolidating prompt management.' **Correctly Intentionally Dormant — do not flag.** |
| grant-engine.monolith.jsx | (all) | Entire monolith file | Intentionally Dormant | Per PRODUCT_CONTEXT.org and DEFERRED.org: standalone reference build for Claude artifacts. Not imported anywhere in the live app. **Do not flag.** |
