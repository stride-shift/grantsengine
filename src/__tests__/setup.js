// Render-net setup: registers @testing-library/jest-dom matchers (toBeInTheDocument,
// toHaveTextContent, etc.) for every test file. Loaded via vitest.config.js setupFiles.
import "@testing-library/jest-dom/vitest";
